# The pybosl2 requirements registry -- the normative source for SPEC.md and PLAN.md.
#
# Generated once from the prose by scripts/extract_requirements.py (T26) and hand-maintained
# since. tests/test_requirements.py keeps it honest: ids are unique across both documents,
# every enforced_by target exists, every citation resolves, and the untriaged backlog only
# shrinks. Ids are permanent (SPEC 13 rule 5) -- a new requirement appends to its series, and
# a withdrawn one keeps its id.
#
# layer:   contract -> SPEC.md, mechanics -> PLAN.md
#
# status:  enforced   (the test in enforced_by fails when the rule is broken)
#          reviewed   (a human checks it at review time; no test can)
#          unenforced (nothing checks it, and the note says what is broken)
#          untriaged  (nobody has decided yet -- the backlog, which only shrinks)
#          withdrawn  (kept for its id, with the reason)
#
# candidates: tests that mention the id. Evidence for triage, never enforcement -- a test
#             that mentions a rule may guard it or may merely cite it, and only reading it
#             tells you which.

[[requirement]]
id = "SPEC-B2-1"
aliases = ["B2-1"]
layer = "contract"
section = "2. Relationship to BOSL2: feature parity, not API parity"
title = "Feature compatible."
statement = '''
Anything BOSL2 can build, pybosl2 aims to build — the same shapes, masks, roundings, sweeps, parts catalogue, and attachment model. Coverage of BOSL2's capabilities is the measure of completeness, and it is **published and checked**, not asserted: `docs/bosl2_coverage.rst` maps every upstream `.scad` file to the module that ports it with a status and a note, generated by `docs/_covgen.py` from a pinned upstream file list and kept honest by `tests/test_bosl2_coverage.py`. A claim of parity that nothing measures is a wish.
'''
enforced_by = [
    "tests/test_bosl2_coverage.py::test_every_named_module_exists",
]
candidates = [
    "tests/test_example_claims.py",
    "tests/test_partitions.py::test_mask_subpaths_keep_bosl2_amplitudes",
    "tests/test_parts_are_lazy.py",
    "tests/test_parts_backend_coverage.py::test_the_measured_coverage_matches_what_the_spec_claims",
]
status = "enforced"

[[requirement]]
id = "SPEC-B2-2"
aliases = ["B2-2"]
layer = "contract"
section = "2. Relationship to BOSL2: feature parity, not API parity"
title = "Not API compatible."
keyword = "MUST NOT"
statement = '''
pybosl2 MUST NOT copy OpenSCAD's calling conventions where Python has a better answer. A ported `.scad` module specifies *behaviour*, not signature:
* objects and methods replace module-with-children,
* chained methods replace transform wrappers,
* enums replace magic strings and numbers,
* exceptions replace `assert`/`echo` diagnostics,
* ambient context (`use_backend`, `use_defaults`) replaces `$`-special variables,
* keyword arguments with real defaults replace long positional lists.
'''
enforced_by = []
candidates = [
    "tests/test_mask_contract.py::test_no_public_masking_signature_mentions_a_native_type",
]
status = "reviewed"
note = '''
The masking signatures are scanned for native types (tests/test_mask_contract.py), which is one module and one symptom. 'Do not copy OpenSCAD's calling conventions' is a design judgement across the whole API, and a reviewer is what applies it.
'''

[[requirement]]
id = "SPEC-B2-3"
aliases = ["B2-3"]
layer = "contract"
section = "2. Relationship to BOSL2: feature parity, not API parity"
title = "Names are kept where they help."
statement = '''
BOSL2's *names* (`cuboid`, `prismoid`, `path_sweep`, `rounding=`, `chamfer=`) are retained so the `.scad` source and the Python read side by side — except where Python forbids the name (`except` → `except_edges`) or a clearer spelling exists.
'''
enforced_by = []
candidates = [
    "tests/test_shape_contract.py::test_a_removed_synonym_does_not_come_back",
]
status = "reviewed"
note = '''
test_a_removed_synonym_does_not_come_back guards C-21, which is adjacent: whether BOSL2's name is the one kept is a judgement about each name, not a property a test can check.
'''

[[requirement]]
id = "SPEC-B2-4"
aliases = ["B2-4"]
layer = "contract"
section = "2. Relationship to BOSL2: feature parity, not API parity"
title = "Departures are documented."
statement = '''
Where behaviour deliberately differs, the docstring says so.
'''
enforced_by = []
status = "reviewed"
note = '''
A docstring rule: where behaviour differs from BOSL2, say so. Nothing reads docstrings for departures.
'''

[[requirement]]
id = "SPEC-P-1"
aliases = ["P-1"]
layer = "contract"
section = "3. Driving principles"
title = "— One required idea per call."
keyword = "MUST NOT"
statement = '''
A constructor SHOULD be usable with one required argument — the thing being made — and MUST NOT require more than two (D-2). Everything else has a default.
'''
enforced_by = [
    "tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain",
]
candidates = [
    "tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain",
    "tests/test_defaults.py::test_facade_never_defaults_an_argument_its_backend_requires",
    "tests/test_defaults.py::test_parts_build_from_their_catalogue_name_alone",
    "tests/test_part_show.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-P-2"
aliases = ["P-2"]
layer = "contract"
section = "3. Driving principles"
title = "— Defaults are the common case, not the neutral case:"
statement = '''
`anchor=CENTER`, `orient=TOP`, `trimcorners=True`, `head=SOCKET`, `thread=COARSE`.
'''
enforced_by = []
candidates = [
    "tests/test_defaults.py::test_ambient_res_reaches_the_sdf_backend",
]
status = "reviewed"
note = '''
Whether a default is 'the common case' rather than the neutral one is a judgement about what callers want. The named ones are asserted individually.
'''

[[requirement]]
id = "SPEC-P-3"
aliases = ["P-3"]
layer = "contract"
section = "3. Driving principles"
title = "— Derive, do not demand."
keyword = "MUST"
statement = '''
Any value computable from the others MUST be optional and computed: thread pitch from the screw spec, gear module from circular pitch, a tube's bore from its wall.
'''
enforced_by = []
candidates = [
    "tests/test_defaults.py::test_size_only_rect_tube_gets_a_wall",
    "tests/test_mask_contract.py::test_a_groove_derives_its_depth_and_length",
    "tests/test_masking_primitives.py::test_mask2d_step_is_square_unless_told_otherwise",
]
status = "reviewed"
note = '''
Spot-checked at rect_tube (test_defaults.py::test_size_only_rect_tube_gets_a_wall). Whether a given value *could* have been derived is a judgement about the domain, not a property of a signature.
'''

[[requirement]]
id = "SPEC-P-4"
aliases = ["P-4"]
layer = "contract"
section = "3. Driving principles"
title = "— Name the thing, not its measurements:"
statement = '''
`Screw("M6", length=20)`, `BallBearings.ball_bearing("608")`, `NemaMotor(size=17)`.
'''
enforced_by = []
candidates = [
    "tests/test_defaults.py::test_parts_build_from_their_catalogue_name_alone",
]
status = "reviewed"
note = '''
Spot-checked by test_parts_build_from_their_catalogue_name_alone, which covers the parts. Naming the thing rather than its measurements is a design judgement elsewhere.
'''

[[requirement]]
id = "SPEC-P-5"
aliases = ["P-5"]
layer = "contract"
section = "3. Driving principles"
title = "— Progressive disclosure."
statement = '''
Positional arguments are for the one or two things everyone supplies; everything else is keyword-only.
'''
enforced_by = [
    "tests/test_signatures.py::test_no_file_grows_its_positional_tier_parameters",
]
status = "enforced"
note = '''
Ratcheted: 114 exported callables still take a tier parameter positionally, and no file may grow its count.
'''

[[requirement]]
id = "SPEC-P-6"
aliases = ["P-6"]
layer = "contract"
section = "3. Driving principles"
title = "— Every default is overridable."
statement = '''
No behaviour is reachable only through a default.
'''
enforced_by = []
status = "reviewed"
note = '''
R-5 gives the ambient defaults their opt-out and that is tested. Whether some *other* behaviour is reachable only through a default is a question about behaviour that does not exist yet.
'''

[[requirement]]
id = "SPEC-P-7"
aliases = ["P-7"]
layer = "contract"
section = "3. Driving principles"
title = "— Errors teach."
keyword = "MUST"
statement = '''
A rejected call MUST say what was wrong *and* what to do instead (§9).
'''
enforced_by = [
    "tests/test_validation_messages.py",
]
status = "enforced"
note = '''
Every rejection path is exercised and its message asserted, which is the rule's second half.
'''

[[requirement]]
id = "SPEC-P-8"
aliases = ["P-8"]
layer = "contract"
section = "3. Driving principles"
title = "— Objects, not argument bags."
keyword = "MUST"
statement = '''
The library is object-oriented by preference: parts, paths, regions, meshes, colours and quaternions are **classes** that own their operations and expose derived values as properties. A family of free functions sharing a prefix and a pile of parameters MUST instead be a class. The *design* is Python's, not a transliteration of `.scad`.
'''
enforced_by = []
candidates = [
    "tests/test_partitions.py::test_bosl2_solid_gets_its_cuts_from_the_partitions_mixin",
    "tests/turtle/test_turtle_methods.py",
]
status = "reviewed"
note = '''
Spot-checked where it was broken (the partitions mixin, test_partitions.py). 'A family of free functions should be a class' is a judgement no scan can make -- prefix-sharing functions are not always a family.
'''

[[requirement]]
id = "SPEC-A-1"
aliases = ["A-1"]
layer = "contract"
section = "4. Architecture"
keyword = "MUST NOT"
statement = '''
A lower layer MUST NOT import a higher one. The layers, their members and the edges that break the rule today are data (`spec/layers.toml`), walked by `tests/test_layering.py`: a new runtime up-import fails, a deferred one is allowed only where it breaks a genuine cycle — which the test resolves in the import graph rather than accepting from a comment (PLAN M-5) — and the list of existing violations only shrinks. Stating this rule for years without measuring it left 18 of them.
'''
enforced_by = [
    "tests/test_layering.py::test_every_runtime_upward_import_is_known_debt",
]
candidates = [
    "tests/test_mask_contract.py::test_no_public_masking_signature_mentions_a_native_type",
]
status = "enforced"
note = '''
Measured for the first time in T29. The layer table this rule referred to could not describe the code even in principle — it placed exceptions/enums/_edges_lang above pure geometry, which every geometry module imports — so the model was corrected before the rule could be applied at all. Against the corrected model: 18 runtime up-edges, now a ratchet in spec/layers.toml, and 10 deferred ones that break a genuine cycle, which the test resolves in the import graph rather than accepting from a comment.
'''

[[requirement]]
id = "SPEC-A-10"
aliases = ["A-10"]
layer = "contract"
section = "4. Architecture"
title = "A geometry object reaches geometry through the façade."
keyword = "MUST"
statement = '''
Where an L2 type builds a shape — `Path2D.polygon()`, `linear_extrude()`, `stroke()`, the sweeps — it MUST build through the L4 façade and return `Flat`/`Solid`, never import a backend module and return that backend's class. Reaching `shapes2d`/`shapes3d` directly is an A-1 violation, leaks an L3 type through an L2 signature, and — since the constructor it reaches is one backend's — silently produces a CSG shape inside `use_backend("sdf")`, which is A-6's defect arriving by a different road and, with the SDF backend a full-parity target, a PAR-1 defect too. S-11 requires a path to *become geometry*; this says which door it goes through.
'''
enforced_by = [
    "tests/test_layering.py::test_every_runtime_upward_import_is_known_debt",
]
status = "enforced"

[[requirement]]
id = "SPEC-A-2"
aliases = ["A-2"]
layer = "contract"
section = "4. Architecture"
keyword = "MUST"
statement = '''
L0 MUST remain usable and tested with no CAD runtime installed.
'''
enforced_by = [
    "tests/test_export.py::test_writing_a_mesh_touches_no_native_module",
]
candidates = [
    "tests/test_export.py::test_writing_a_mesh_touches_no_native_module",
    "tests/test_no_mesher.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-A-3"
aliases = ["A-3"]
layer = "contract"
section = "4. Architecture"
statement = '''
New geometry algorithms belong in L0, with native call sites isolated in L2.
'''
enforced_by = []
status = "reviewed"
note = '''
Where a *new* algorithm belongs is a review question. A-1's layer test catches it only once it has imported upwards.
'''

[[requirement]]
id = "SPEC-A-4"
aliases = ["A-4"]
layer = "contract"
section = "4. Architecture"
keyword = "MUST"
statement = '''
`import pybosl2` MUST stay cheap: top-level names resolve lazily, and every lazily exported name MUST still be statically declared so editors and type checkers see the full API.
'''
enforced_by = [
    "tests/test_init_stub.py::test_stub_exists",
]
candidates = [
    "tests/test_docs_links.py::test_import_pybosl2_needs_no_optional_dependency",
    "tests/test_init_stub.py::test_stub_exists",
]
status = "enforced"

[[requirement]]
id = "SPEC-A-5"
aliases = ["A-5"]
layer = "contract"
section = "4. Architecture"
keyword = "MUST NOT"
statement = '''
`square`/`circle`/`cube`/`text` intentionally shadow the OpenSCAD builtins with the anchor-aware versions, so the package MUST NOT be wildcard re-exported.
'''
enforced_by = [
    "tests/test_declared_surface.py::test_the_package_is_not_wildcard_re_exported",
]
status = "enforced"
note = '''
Both halves: no wildcard import inside the package, and the four shadowing names still resolve to this library's own.
'''

[[requirement]]
id = "SPEC-A-6"
aliases = ["A-6"]
layer = "contract"
section = "4. Architecture"
keyword = "MUST"
statement = '''
Everything exported from the top level MUST honour the active backend. A constructor that only one backend can build belongs on that backend's module, not on `pybosl2`, so that `from pybosl2 import …` never silently produces a shape on the backend the caller did not select. Mixed neutral and backend-specific names at the top level is the defect this rule exists to prevent.
'''
enforced_by = [
    "tests/test_exports.py::test_no_top_level_name_builds_on_the_wrong_backend",
]
candidates = [
    "tests/test_backend_matrix.py::test_path_2d_geometry_builds_on_either_backend",
]
status = "enforced"
note = '''
The top-level export table was always checked; the geometry bridges were not, so Path2D.polygon() returned a CSG shape inside an sdf block whatever the caller had selected. Fixed in T29 by A-10 (build through the façade), and covered by the candidate test above as well as the export walk.
'''

[[requirement]]
id = "SPEC-A-7"
aliases = ["A-7"]
layer = "contract"
section = "4. Architecture"
keyword = "MUST"
statement = '''
A name in a module's `__all__` MUST resolve. An `__all__` entry with no matching attribute breaks `import *` and every tool that walks the export list.
'''
enforced_by = [
    "tests/test_exports.py::test_every_all_entry_resolves",
]
candidates = [
    "tests/test_exports.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-A-8"
aliases = ["A-8"]
layer = "contract"
section = "4. Architecture"
title = "A family is exported whole."
keyword = "MUST"
statement = '''
Where the top level exports part of a named family, it exports all of it: five `mask2d_*` without `mask2d_roundover`, or the sweep enums without `SweepMethod`, is drift rather than a decision, and it costs the caller an import hunt at exactly the moment the design was meant to be paying off. The families that MUST be reachable from `pybosl2` are the ones the spec names as the way to do something: the sweeps (S-19), their enums and the `os_*` end treatments (S-20, S-21), the masks (S-26), the turtles (S-10), `Quaternion` (S-5), and the corner-treatment entry points (S-8). Checked by a test, not by review.
'''
enforced_by = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
candidates = [
    "tests/test_exports.py::test_polyline_ratchet_would_catch_a_raw_nesting",
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-A-9"
aliases = ["A-9"]
layer = "contract"
section = "4. Architecture"
title = "The top level exports the API, not the plumbing."
keyword = "MUST NOT"
statement = '''
An internal helper — argument forwarding, signature filtering, registry access — MUST NOT appear in a public `__all__`. `given_arguments` is such a helper.
'''
enforced_by = [
    "tests/test_exports.py::test_no_public_all_advertises_internal_plumbing",
]
candidates = [
    "tests/test_exports.py::test_no_public_all_advertises_internal_plumbing",
    "tests/test_exports.py::test_polyline_ratchet_would_catch_a_raw_nesting",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-15"
aliases = ["C-15"]
layer = "contract"
section = "5.1 Shapes"
title = "One contract, two specialisations."
keyword = "MUST"
statement = '''
`Shape` declares everything true of every shape on every backend: the `backend` tag, the boolean operators, the transforms, `bounds()`, and `show()` (§6.16). `Flat` and `Solid` extend it and add **only** what is genuinely dimensional. A member that both would declare identically MUST live on `Shape` instead — that duplication is how `Flat` came to be missing `bounds()` (S-2) and to be typed `Any` long after `Solid` was not.
'''
enforced_by = [
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
]
candidates = [
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
    "tests/test_sdf_flat_parity.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-16"
aliases = ["C-16"]
layer = "contract"
section = "5.1 Shapes"
title = "Operations preserve the kind."
keyword = "MUST"
statement = '''
Every operation on a shape returns *the same kind of shape*, and an operand of a boolean MUST be the same kind: `flat | flat` and `solid | solid` are the only spellings, and `flat | solid` is a static error, not a runtime surprise. This is C-4 enforced by the contract rather than by convention.
'''
enforced_by = [
    "tests/test_error_contract.py::test_mixing_dimensions_raises_and_names_the_way_across",
]
candidates = [
    "tests/test_error_contract.py::test_mixing_dimensions_raises_and_names_the_way_across",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-17"
aliases = ["C-17"]
layer = "contract"
section = "5.1 Shapes"
title = "Crossing dimensions is explicit and named."
statement = '''
`Flat` → `Solid` happens only through an extrusion or sweep; `Solid` → `Flat` only through `projection`. Each direction is a method a reader can see at the call site; neither happens implicitly.
'''
enforced_by = [
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
]
candidates = [
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
    "tests/test_sdf_flat_parity.py::test_minkowski_refuses_and_names_the_thing_that_does_work",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-18"
aliases = ["C-18"]
layer = "contract"
section = "5.1 Shapes"
title = "One declaration site, one name each."
statement = '''
All three protocols are declared together in the backend-contract layer (L1), so a change to the shared surface cannot land on one dimension and miss the other. `Shape`, `Flat` and `Solid` are the only names for them — no aliases: a second spelling of a contract is a second thing to keep in step, and `Shape2D` was exactly that.
'''
enforced_by = [
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
]
candidates = [
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-19"
aliases = ["C-19"]
layer = "contract"
section = "5.1 Shapes"
title = "Colour and distribution belong to the shared contract."
keyword = "SHOULD"
statement = '''
Both are operations on "any shape", not on solids specifically, so they SHOULD be members of `Shape` (S-37, S-31). They are universal now: the SDF backend's solids carry colour as field metadata, and its 2-D shapes gained the same shared mixins the solids had (`Colorable`, `Anchorable`, `Distributable`). The gap was never a limit of distance fields — colour is metadata, distribution is matrices, and anchoring needs only a bounding box, which an SDF carries exactly. It was the 2-D shape having inherited none of it.
'''
enforced_by = [
    "tests/test_backend_parity.py::test_colour_and_modifiers_are_recorded_on_the_field",
]
candidates = [
    "tests/test_backend_parity.py::test_colour_and_modifiers_are_recorded_on_the_field",
    "tests/test_regions.py::test_geometry_preserves_color",
    "tests/test_sdf_flat_parity.py::test_colour_does_not_leak_between_copies",
    "tests/test_sdf_flat_parity.py::test_the_sdf_flat_shape_satisfies_the_contract",
    "tests/test_shape_contract.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-20"
aliases = ["C-20"]
layer = "contract"
section = "5.1 Shapes"
title = "The contract is the whole object, and it is measured."
keyword = "MUST"
statement = '''
Every public operation a shape implements MUST be declared on `Shape`, `Flat` or `Solid`. A protocol that declares a third of what the object does is not a contract — it is a subset that silently locks typed callers out of the rest, and a caller who follows this library's own typing advice (PLAN §2) is exactly who it locks out. `def with_boss(base: Solid) -> Solid: return base.attach(...)` MUST type-check; attachment, tagging, distribution, the edge treatments, the half-cuts and the colour methods are the library's headline composition features and none of them may be reachable only by accident of the concrete class. Conformance is **checked, not asserted**: a contract test walks the public attributes of every implementation and fails on any name the protocols do not declare, with a named, justified allowlist for the deliberate exceptions. Drift between the declared surface and the delivered one is the defect this rule exists to prevent — it is how `Flat` came to be missing `bounds()` (S-2), and it recurred at scale immediately after C-15 closed.
'''
enforced_by = [
    "tests/test_shape_contract.py::test_the_contract_is_the_whole_object",
]
candidates = [
    "tests/test_sdf_flat_parity.py::test_the_sdf_flat_shape_satisfies_the_contract",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-21"
aliases = ["C-21"]
layer = "contract"
section = "5.1 Shapes"
title = "One operation, one name."
statement = '''
A shape operation has exactly one public spelling. Synonym pairs — `move`/`translate`, `rot`/`rotate`, `fwd`/`forward`, `bounding_box`/`bounds`, `nominal_size`/`size` — double the surface a reader has to learn and halve the chance two call sites are written the same way, and each one is a second thing to keep in step. Where BOSL2's name and Python's differ, keep BOSL2's (B2-3) and drop the other. This is deliberately narrower than D-5: two spellings of a *dimension* (`radius`/`diameter`) are a convenience the caller chooses between, whereas two spellings of an *operation* are the same call twice.
'''
enforced_by = [
    "tests/test_shape_contract.py::test_no_public_class_carries_both_spellings_of_one_operation",
]
candidates = [
    "tests/test_backend_parity.py::test_every_directional_move_stays_in_the_field",
    "tests/test_bounds_contract.py::test_a_shape_has_one_public_name_for_its_nominal_box",
    "tests/test_shape_contract.py::test_a_removed_synonym_does_not_come_back",
    "tests/test_shapes3d.py::test_rotate_has_one_spelling",
]
status = "enforced"
note = '''
Closed on the shape surface in T17 and open everywhere the test did not look: Path2D and Path3D each carried three synonym pairs until T32 — deduplicate/deduplicated, subdivide/subdivide_path, resample/resample_path. The check now walks every public class reachable from the top level, not one class family.

**One concept, one name is a rule about *rules* as much as about names.** T40 found `center=` resolved into an anchor in **eleven** places across the CSG backend, in three spellings and **two contradicting precedences**. Seven gave `center` precedence, which is BOSL2's rule -- six through a private helper and one spelling the same thing out. The other four spelled it inline in a form that lets `anchor` win — while their own docstrings said "center: if given, overrides anchor". `cyl(height=10, radius=5, anchor=TOP, center=False)` sat on its top face with the documentation beside it saying it would sit on the bottom one (E-5). Nothing here compares two implementations of one rule, which is why this went unseen while the *names* were being deduplicated; what closed it was needing the rule a twelfth time, for the SDF backend, and putting it in `pybosl2.groups.resolve_center_anchor` instead.
'''

[[requirement]]
id = "SPEC-C-22"
aliases = ["C-22"]
layer = "contract"
section = "5.1 Shapes"
title = "The dimensional split is earned, not historical."
keyword = "MUST"
statement = '''
A member present on one of `Flat`/ `Solid` and absent from the other MUST be genuinely dimensional (C-15). `xflip`/`yflip` on `Flat` alone, `spin` on `Flat` alone, `up`/`down` on `Solid` alone are the shape of the defect: nothing about two dimensions forbids the first two, and nothing about three requires a separate name for the third. A member that both dimensions can honour belongs on `Shape`.
'''
enforced_by = []
candidates = [
    "tests/test_sdf_flat_parity.py::test_distribute_on_path_unions_the_copies_into_one_shape",
    "tests/test_shape_contract.py::test_the_contract_is_the_whole_object",
]
status = "reviewed"
note = '''
The contract test walks what each protocol declares, but nothing asks whether a member landing on `Flat` alone is *genuinely dimensional* -- that is a judgement about the operation, which is why xflip/yflip sat on Flat alone for so long.
'''

[[requirement]]
id = "SPEC-C-23"
aliases = ["C-23"]
layer = "contract"
section = "5.1 Shapes"
title = "A contract member is typed, or it is on a shrinking list."
keyword = "MUST"
statement = '''
C-20 requires the contract to cover the whole object; this requires the cover to mean something. A member declared `(*args: Any, **kwargs: Any)` is present by name and checked not at all — the method is found and every argument is accepted, including the wrong ones, which is worse than a missing member because it reads as a promise. Where the two backends' concrete signatures genuinely differ in a way no checker can reconcile, the protocol MAY declare the caller's view with `Any` (PLAN T-6c); that is a bounded list that only shrinks, not a default. What is *not* that case, and MUST be written out rather than listed, is a member whose implementations already agree — which was 23 of the first 48, the whole distribution and attachment families. Note what does **not** justify a loose declaration: an implementation that is itself loose. A CSG-only feature refused on the SDF backend by a `(*_args: Any, **_kwargs: Any)` stub (PAR-3) satisfies *any* protocol signature, so the contract is free to declare the real one; the loose stub is a fact about the refusal, not a constraint on the contract. Reading it as a constraint is what left the attachment family unchecked.
'''
enforced_by = [
    "tests/test_contract_typing.py::test_no_new_loosely_typed_member",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-1"
aliases = ["C-1"]
layer = "contract"
section = "5.1 Shapes"
statement = '''
Every shape carries a `backend` tag naming the backend that **produced** it. It is a property of the object, never a reading of the ambient backend at construction time — a CSG shape built inside a `use_backend("sdf")` block is still a CSG shape, and must say so. Booleans and transforms return a shape on the same backend; mixing raises `CrossBackendError` naming the conversion that fixes it. A tag that reports the ambient backend defeats that guard entirely and turns a clear error into an internal one.
'''
enforced_by = [
    "tests/test_backend_matrix.py::test_a_csg_shape_built_inside_an_sdf_block_still_says_csg",
]
candidates = [
    "tests/test_backend_matrix.py::test_2d_shape_constructors_refuse_on_another_backend",
    "tests/test_backend_matrix.py::test_a_csg_shape_built_inside_an_sdf_block_still_says_csg",
    "tests/test_backend_parity.py::test_every_directional_move_stays_in_the_field",
    "tests/test_backend_parity.py::test_sdf_shapes_keep_their_backend_through_moves_and_colour",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-2"
aliases = ["C-2"]
layer = "contract"
section = "5.1 Shapes"
statement = '''
Operators are the primary spelling: `|` union, `&` intersection, `-` difference.
'''
enforced_by = [
    "tests/test_shapes3d.py",
]
status = "enforced"
note = '''
The operators are exercised across the shape surface; a shape without them fails immediately.
'''

[[requirement]]
id = "SPEC-C-3"
aliases = ["C-3"]
layer = "contract"
section = "5.1 Shapes"
statement = '''
Shapes are immutable by convention: every operation returns a new shape, so calls chain. `attach()` and `tag()` copy rather than mutate; the `attachments` / `tag_name` setters exist for those copies to use and are not part of the public contract.
'''
enforced_by = []
status = "reviewed"
note = '''
Immutability is 'by convention' in the rule's own words. Individual operations are checked to return new objects; nothing walks the surface asserting no method mutates self.
'''

[[requirement]]
id = "SPEC-C-4"
aliases = ["C-4"]
layer = "contract"
section = "5.1 Shapes"
statement = '''
2-D and 3-D never mix implicitly; a flat shape reaches 3-D only through an explicit extrude or sweep, and the contract itself enforces it (C-16, C-17).
'''
enforced_by = [
    "tests/test_error_contract.py::test_mixing_dimensions_raises_and_names_the_way_across",
]
candidates = [
    "tests/test_error_contract.py::test_mixing_dimensions_raises_and_names_the_way_across",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-5"
aliases = ["C-5"]
layer = "contract"
section = "5.1 Shapes"
statement = '''
Shared behaviour (transforms, directional moves, CSG, colour, tags, distribution) lives once in the common base, not duplicated per dimension.
'''
enforced_by = [
    "tests/test_shape_contract.py::test_the_contract_is_the_whole_object",
]
status = "enforced"
note = '''
A member duplicated per dimension shows up as a contract mismatch, which is how O-1c's duplicate mixin was found.
'''

[[requirement]]
id = "SPEC-C-6"
aliases = ["C-6"]
layer = "contract"
section = "5.1 Shapes"
keyword = "MUST"
statement = '''
Forwarding to a native object is limited to an explicit allowlist, and every forwarded name MUST exist on the wrapped object and be statically declared.
'''
enforced_by = [
    "tests/test_declared_surface.py::test_every_forwarded_name_exists_on_the_wrapped_object",
]
status = "enforced"
note = '''
`roof` is named as the one app-only op the pip wheel lacks, which is why the render tests skip rather than fail.
'''

[[requirement]]
id = "SPEC-C-7"
aliases = ["C-7"]
layer = "contract"
section = "5.2 Geometry objects"
keyword = "MUST"
statement = '''
`Path` is abstract; `Path2D`/`Path3D` are selected by point dimension. Any API taking a polyline MUST accept a `Path` — and MUST say so in its signature, not merely tolerate one at runtime: a caller feeding one function's result to the next is the normal way to use the library, and it must type-check ([PLAN T-4](PLAN.md#2-typing)).
'''
enforced_by = [
    "tests/test_exports.py::test_every_polyline_parameter_accepts_a_path",
]
candidates = [
    "tests/test_exports.py::test_every_polyline_parameter_accepts_a_path",
    "tests/test_exports.py::test_no_top_level_name_builds_on_the_wrong_backend",
    "tests/test_polyline_parameters.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-7a"
aliases = ["C-7a"]
layer = "contract"
section = "5.2 Geometry objects"
keyword = "MUST NOT"
statement = '''
A public parameter that means *an ordered set of points* — a path, outline, profile, polygon or point list — MUST be typed as `Path2D`, `Path3D` or `Path`, and MUST NOT accept a bare `Sequence[Sequence[float]]` or a NumPy array. **The point type is the contract, not a convenience wrapper over one.** A bare sequence carries no dimension, no open/closed flag and no winding, so each function that takes one has to re-derive all three, and they do not always agree — the same list is a 2-D outline to one function and a degenerate 3-D path to the next. Requiring the type moves that decision to the one place that can make it once, at construction. A caller with raw points writes `Path2D(pts)`, which is where the points get checked.
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_no_new_parameter_takes_raw_points",
]
candidates = [
    "tests/test_polyline_parameters.py::test_a_path_annotation_is_backed_by_a_guard",
    "tests/test_polyline_parameters.py::test_as_path_list_no_longer_guesses_one_path_from_many",
    "tests/test_polyline_parameters.py::test_path_copies_reads_closed_off_the_path_instead_of_guessing",
    "tests/test_polyline_parameters.py::test_the_closure_helpers_refuse_raw_points",
    "tests/test_polyline_parameters.py::test_the_guard_checks_the_width_not_merely_that_it_is_a_path",
    "tests/test_polyline_parameters.py::test_the_last_tranche_refuses_raw_points",
    "tests/test_validation_messages.py::test_extrusion_and_text_rejections",
    "tests/test_vnf.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-7d"
aliases = ["C-7d"]
layer = "contract"
section = "5.2 Geometry objects"
keyword = "MUST"
statement = '''
Where only one width will do, the check MUST be for the *concrete* type, not for `Path`. `Path2D` and `Path3D` are siblings, so a guard that asks only "is this a `Path`?" lets a `Path3D` satisfy a `Path2D` parameter and reach a formula that reads columns 0 and 1 and drops z — a wrong answer rather than a refusal, which is worse than the bare sequence this requirement set out to remove, because it is silent. `require_path(..., expect=Path2D)` performs it, and says how to convert.
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_a_path_annotation_is_backed_by_a_guard",
]
candidates = [
    "tests/test_polyline_parameters.py::test_polygon_area_is_planar_and_says_so",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-7b"
aliases = ["C-7b"]
layer = "contract"
section = "5.2 Geometry objects"
keyword = "MUST"
statement = '''
The refusal MUST name the wrapper to apply. Raw points are what a newcomer has — literals, a CSV, another library's output — so `polygon([[0,0],[1,0],[1,1]])` is the natural first attempt, and the error it produces is the library's chance to teach the type rather than merely reject the call (E-4).
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_the_refusal_names_the_wrapper_to_apply",
]
candidates = [
    "tests/test_polyline_parameters.py::test_the_refusal_names_the_wrapper_to_apply",
    "tests/test_polyline_parameters.py::test_the_sdf_2d_entry_points_refuse_raw_points",
    "tests/test_polyline_parameters.py::test_the_sdf_path_utilities_refuse_raw_points",
    "tests/test_validation_messages.py::test_polygon_prism_names_the_wrapper_for_a_non_path",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-7c"
aliases = ["C-7c"]
layer = "contract"
section = "5.2 Geometry objects"
statement = '''
`PathLike` remains, but is **internal**: it types the normalisation step inside a function body, never a public parameter. Its docstring said it was "anything an API that wants a polyline accepts", which made the permissive form look like the intended contract at every call site that imported it.
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_no_new_parameter_takes_raw_points",
]
candidates = [
    "tests/test_polyline_parameters.py",
]
status = "enforced"
note = '''
PathLike in a public signature is what the ratchet counts.
'''

[[requirement]]
id = "SPEC-C-8"
aliases = ["C-8"]
layer = "contract"
section = "5.2 Geometry objects"
keyword = "SHOULD"
statement = '''
`Region` is outlines-with-holes; `VNF` is the vertex/face mesh interchange type. Anything that can produce a mesh SHOULD be able to produce a `VNF`, so it can be inspected and measured with no CAD runtime present.
'''
enforced_by = [
    "tests/test_vnf.py",
]
candidates = [
    "tests/test_sweep_contract.py::test_every_sweep_keeps_its_mesh_reachable",
    "tests/test_texture.py::test_the_tile_functions_trade_in_meshes_not_halves",
    "tests/test_vnf.py::test_is_watertight_on_a_closed_and_an_open_mesh",
]
status = "enforced"
note = '''
VNF is the interchange type and is exercised as one; `.vnf()` on the sweeps is separately checked.
'''

[[requirement]]
id = "SPEC-C-9"
aliases = ["C-9"]
layer = "contract"
section = "5.2 Geometry objects"
statement = '''
Geometry objects own their operations as methods and return new objects (C-3).
'''
enforced_by = []
status = "reviewed"
note = '''
True of the geometry types today and tested per type, but nothing requires a *new* method to return a new object rather than mutate.
'''

[[requirement]]
id = "SPEC-C-10"
aliases = ["C-10"]
layer = "contract"
section = "5.3 The anchor language"
keyword = "MUST NOT"
statement = '''
One enum (`Anchor`) covers faces, edges, corners, and axis presets, each member carrying its own vector. Any "which face/edge/corner" parameter MUST accept it; new APIs MUST NOT invent a parallel string or integer vocabulary.
'''
enforced_by = [
    "tests/test_anchor.py",
]
candidates = [
    "tests/test_sdf_flat_parity.py::test_distribute_on_path_unions_the_copies_into_one_shape",
]
status = "enforced"
note = '''
The anchor language is exercised across faces, edges, corners and axis presets.
'''

[[requirement]]
id = "SPEC-C-11"
aliases = ["C-11"]
layer = "contract"
section = "5.3 The anchor language"
statement = '''
The `constants.py` names (`TOP`, `LEFT`, `CENTER`, …) are aliases kept for BOSL2 familiarity; `Anchor.TOP` is preferred in new code and examples.
'''
enforced_by = [
    "tests/test_constants.py",
]
status = "enforced"
note = '''
The constants are asserted to be the Anchor members they alias.
'''

[[requirement]]
id = "SPEC-C-12"
aliases = ["C-12"]
layer = "contract"
section = "5.4 Attachment"
statement = '''
A solid can carry attached children, positioned by anchor (`attach`, `position`, `align`, `reorient`, `orient`), tagged for boolean resolution (`AttachTag.KEEP` / `REMOVE` / `INTERSECT`), and resolved with `diff()` / `intersect()` at realize time.
'''
enforced_by = [
    "tests/test_attachments.py",
]
status = "enforced"
note = '''
Attachment, tagging and the boolean resolution are exercised end to end.
'''

[[requirement]]
id = "SPEC-C-13"
aliases = ["C-13"]
layer = "contract"
section = "5.4 Attachment"
statement = '''
Attachment is a CSG-backend feature; the SDF backend refuses it explicitly (§7.1).
'''
enforced_by = [
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
]
candidates = [
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-14"
aliases = ["C-14"]
layer = "contract"
section = "5.5 Parts"
statement = '''
Every part is a class (P-8) that resolves its inputs into a frozen **spec object**, exposes derived dimensions as read-only properties, builds geometry lazily under a `shape` **property** (`Screw("M6", length=20).shape`, not `.shape()`), and offers `show()`. Callers can therefore *measure* a part without building it.
'''
enforced_by = [
    "tests/test_exports.py::test_every_part_exposes_shape_as_a_property",
]
candidates = [
    "tests/test_exports.py::test_every_part_exposes_shape_as_a_property",
    "tests/test_parts_are_lazy.py::test_a_lazy_part_answers_its_catalogue_without_building",
    "tests/test_parts_are_lazy.py::test_no_part_becomes_eager",
    "tests/test_parts_are_lazy.py::test_reading_a_derived_property_never_triggers_a_build",
]
status = "enforced"

[[requirement]]
id = "SPEC-C-14a"
aliases = ["C-14a"]
layer = "contract"
section = "5.5 Parts"
keyword = "MUST"
statement = '''
Note the deliberate difference in what `.shape` means either side of the FFI boundary: on a part it is the finished `Solid`/`Flat`; on a backend wrapper (`CsgSolid.shape`) it is the raw native handle being wrapped. Docstrings MUST say which one they mean.
'''
enforced_by = []
status = "reviewed"
note = '''
A docstring rule: that each `.shape` says which side of the FFI it means. Nothing reads docstrings for it.
'''

[[requirement]]
id = "SPEC-S-1"
aliases = ["S-1"]
layer = "contract"
section = "6.1 Maths, points and vectors"
statement = '''
Scalar and vector helpers (`slerp`, `modang`, `quant`, `constrain`, `mean`, `EPSILON`) mirror BOSL2's maths, implemented on Python/NumPy primitives rather than re-derived.
'''
enforced_by = [
    "tests/test_math.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-2"
aliases = ["S-2"]
layer = "contract"
section = "6.1 Maths, points and vectors"
keyword = "MUST"
statement = '''
`Point`/`Vector` are the point types; `Bounds2D`/`Bounds3D` are the axis-aligned box types. Every shape and mesh MUST report its bounds without rendering — this is part of both the `Solid` and the `Flat` contract, not just the 3-D one.
'''
enforced_by = [
    "tests/test_bounds_contract.py::test_every_bounds_is_a_bounds_object",
]
status = "enforced"
note = '''
Every type the rule names is walked -- shapes on both backends, Path2D/Path3D, Region and VNF -- and each is required to answer a real box. **The note this row carried until T39 was PLAN-S-2's**, about functions over 50 lines: T26's confirmed-enforcement table was keyed by the bare id, so `S-2` matched whichever entry came first. That is the collision the prefixed ids exist to prevent, landing in the data written to record it.
'''

[[requirement]]
id = "SPEC-S-2b"
aliases = ["S-2b"]
layer = "contract"
section = "6.1 Maths, points and vectors"
title = "One box type, one meaning."
keyword = "MUST NOT"
statement = '''
`bounds()` MUST return a `Bounds2D` or a `Bounds3D` — everywhere, on every type that has one: shapes on either backend, `Path2D`/`Path3D`, `Region`, `VNF`. It MUST NOT return a bare tuple, a NumPy array, or any other pair whose halves the caller has to guess at. `lo, hi = thing.bounds()` is the obvious reading of the name, and a method that answers a *centre* and a *size* to that read is a silent wrong answer, not an API. The box carries both spellings as properties — `min_x`…`max_z`, `center`, `size`, `width`/`length` /`height` — so no caller has to do the arithmetic and no implementation has to pick a winner. The raw backend pair stays private (`_native_bounds()`); the public name means one thing. A second public spelling of the same box on the same object (`bounding_box()` beside `bounds()`, `nominal_size` beside `size`) is the drift this rule exists to prevent — one concept, one name (C-21).
'''
enforced_by = [
    "tests/test_bounds_contract.py::test_every_bounds_is_a_bounds_object",
]
candidates = [
    "tests/test_bounds_contract.py::test_the_box_carries_every_spelling_of_itself",
    "tests/test_bounds_contract.py::test_unpacking_a_box_fails_loudly",
    "tests/test_option_parity.py::test_a_spin_about_z_leaves_a_round_shape_where_it_was",
]
status = "enforced"
note = '''
The rule is about the box's *type*, and one thing about its *value* is recorded here because T40 made it reachable. A shape that is round about Z reports a **looser box after a spin that cannot move it** -- `sphere(spin=30)` says 27.3 across where it is still 20 -- because `PyShape.rotate` recomputes the bound as the axis-aligned box of the rotated *corner box*: exact for a cuboid, loose for a disc. The field is untouched (rotating `|p| - r` gives back `|p| - r`); only the stored bound grew. Pre-existing in `rotate`, and invisible until T40 gave the SDF constructors `spin`. `ROUND_ABOUT_Z` in `tests/test_option_parity.py` lists the shapes and a test fails if the list goes stale, so one comes off it rather than sitting there as an excuse. Fixing it means giving `rotate` a bound that tracks the shape rather than its corner box, which touches every rotated shape and is its own task.

The general point is worth more than the case: an SDF shape's `bounds()` is the box it **declares**, not a measurement of its field. A cross-backend bounds comparison therefore cannot see a defect that moves the geometry and leaves the declaration alone -- T40 found one (the `shift` shear plane, PAR-5) that a bounds test passes with the defect put back.
'''

[[requirement]]
id = "SPEC-S-2a"
aliases = ["S-2a"]
layer = "contract"
section = "6.1 Maths, points and vectors"
keyword = "MUST NOT"
statement = '''
A shape's `size` is its **nominal anchor box**, and is not required to equal its bounding box. `anchor=` is measured against the box the shape is *designed* around — a gear's pitch circle, a regular polyhedron's circumsphere, the plate a snap fitting mounts on — so real geometry may sit inside it (a polyhedron inside its circumsphere) or stand outside it (gear teeth past the pitch circle, a truss clip's hooks past the cell it grips). `bounds()` reports the geometry and MUST prefer the native bounding box; it falls back to `size` only where no native box is available. A part whose `size` differs from its `bounds()` MUST say at the declaration which box it is naming and why. Tests MUST NOT assert that the two agree.
'''
enforced_by = [
    "tests/test_dimension_validation.py",
]
candidates = [
    "tests/test_anchoring_parity.py::test_reanchor_records_the_anchor_where_a_nominal_box_exists",
    "tests/test_bounds_contract.py::test_bounds_tracks_the_geometry_through_a_boolean",
    "tests/test_nominal_anchor_box.py::test_a_differing_nominal_box_is_explained_at_its_declaration",
    "tests/test_nominal_anchor_box.py::test_bounds_reports_geometry_not_the_nominal_box",
    "tests/test_nominal_anchor_box.py::test_the_list_matches_the_parts_that_actually_differ",
    "tests/test_nominal_anchor_box.py::test_the_spec_and_plan_state_the_rule",
    "tests/test_nominal_size_api.py::test_the_nominal_box_is_attached_without_touching_the_geometry",
    "tests/test_sdf_flat_parity.py::test_distribute_on_path_unions_the_copies_into_one_shape",
    "tests/test_sdf_flat_parity.py::test_the_nominal_box_is_carried_and_is_not_the_measurement",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-3"
aliases = ["S-3"]
layer = "contract"
section = "6.1 Maths, points and vectors"
statement = '''
Line/plane/polygon predicates and intersections (`geometry`) operate on `Point`s and `Path`s, and honour the `SEGMENT`/`RAY`/`LINE` specifiers.
'''
enforced_by = [
    "tests/test_paths.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-4"
aliases = ["S-4"]
layer = "contract"
section = "6.2 Transforms and rotations"
statement = '''
Affine transforms are 4×4 matrices; the library provides construction (`axis_angle_matrix`, `rot_from_to`, `rot_about_axis`), inversion, decoding, and application (`apply`, `reorient`).
'''
enforced_by = [
    "tests/test_transforms.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-5"
aliases = ["S-5"]
layer = "contract"
section = "6.2 Transforms and rotations"
statement = '''
`Quaternion` is a class with the full rotation algebra (multiply, slerp, to-matrix, to-axis, rotate-point), and is the preferred spelling for interpolated rotation.
'''
enforced_by = [
    "tests/test_exports.py",
]
candidates = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-6"
aliases = ["S-6"]
layer = "contract"
section = "6.2 Transforms and rotations"
statement = '''
Shapes expose the common transforms as chainable methods — `translate`, `rotate`, `scale`, `mirror`, `multmatrix`, and the directional moves `up`/`down`/`left`/`right`/ `forward`/`back`.
'''
enforced_by = [
    "tests/test_backend_parity.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-7"
aliases = ["S-7"]
layer = "contract"
section = "6.3 Paths and curves"
statement = '''
A `Path` is an ordered point list with an open/closed flag, and owns its measurement (length, perimeter, tangents, normals, curvature, torsion), its sampling (`subdivide_path`, `resample_path`, `select`, `cut`, `closest_point`), and its cleanup (`deduplicate`, `merge_collinear`).
'''
enforced_by = [
    "tests/test_paths.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-8"
aliases = ["S-8"]
layer = "contract"
section = "6.3 Paths and curves"
keyword = "MUST"
statement = '''
Corner treatment (`round_corners`, `smooth_path`) MUST support BOSL2's rounding methods (circle / smooth / chamfer) and size measures (radius / cut / joint / width) as enums.
'''
enforced_by = [
    "tests/test_rounding.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-9"
aliases = ["S-9"]
layer = "contract"
section = "6.3 Paths and curves"
statement = '''
Bezier curves and patches (`Bezier`, `BezierPatch`) and NURBS curves and patches (`NurbsCurve`, `NurbsPatch`) are classes that evaluate, subdivide, and convert to `Path`s or `VNF`s, so they interoperate with everything else.
'''
enforced_by = [
    "tests/test_validation_messages.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-10"
aliases = ["S-10"]
layer = "contract"
section = "6.3 Paths and curves"
statement = '''
Turtle graphics (`turtle2d`, `turtle3d`) build paths from a command list, and their output is an ordinary `Path`.
'''
enforced_by = [
    "tests/test_documented_arguments.py",
]
candidates = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-11"
aliases = ["S-11"]
layer = "contract"
section = "6.3 Paths and curves"
keyword = "MUST"
statement = '''
A path MUST be able to become geometry: polygon/region fill, extrusion, sweep, or stroke (§6.7). "Become geometry" means it returns a **shape** — the caller asked for a solid and must get one, not a mesh plus an incantation (S-19a).
'''
enforced_by = [
    "tests/test_paths.py",
]
status = "enforced"
note = '''
The path-to-geometry bridge is exercised for polygon, region, extrusion and stroke; T29 also made it return the contract type.
'''

[[requirement]]
id = "SPEC-S-12"
aliases = ["S-12"]
layer = "contract"
section = "6.4 Regions"
statement = '''
A `Region` is a set of outlines with holes, supporting boolean union/difference/ intersection, offset, and decomposition back into `Path2D`s.
'''
enforced_by = [
    "tests/test_rounding.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-13"
aliases = ["S-13"]
layer = "contract"
section = "6.4 Regions"
keyword = "MUST"
statement = '''
Region booleans MUST be exact and independent of any CAD runtime.
'''
enforced_by = [
    "tests/test_regions.py",
]
status = "enforced"
note = '''
Region booleans are tested with no CAD runtime, which is both halves of the rule.
'''

[[requirement]]
id = "SPEC-S-14"
aliases = ["S-14"]
layer = "contract"
section = "6.4 Regions"
keyword = "MUST"
statement = '''
A `Region` MUST be able to render as a 2-D shape or extrude to a solid, and to carry colour through that conversion (§6.12).
'''
enforced_by = [
    "tests/test_rounding.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-15"
aliases = ["S-15"]
layer = "contract"
section = "6.5 Meshes and surfaces"
statement = '''
`VNF` (vertices + faces) is the mesh interchange type: constructed from grids (`vertex_array`, `tri_array`), from fields (`from_field`), from metaballs (`from_metaballs`), or from skinned profiles (`from_skin`); combined (`union`, `join`); cut (`halfspace`, `slice`); measured (`bounds`, `volume`); and realized (`polyhedron`).
'''
enforced_by = [
    "tests/test_vnf.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-16"
aliases = ["S-16"]
layer = "contract"
section = "6.5 Meshes and surfaces"
statement = '''
Height fields and function plots (`heightfield`, `cylindrical_heightfield`, `plot3d`, `plot_revolution`) produce meshes from data or a callable, with an explicit sampling range.
'''
enforced_by = [
    "tests/test_validation_messages.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-17"
aliases = ["S-17"]
layer = "contract"
section = "6.5 Meshes and surfaces"
keyword = "MUST"
statement = '''
Implicit surfaces — metaballs (`mb_sphere`, `mb_cuboid`, `mb_torus`, `mb_capsule`, `mb_disk`, `mb_octahedron`, `mb_connector`), `metaballs2d`, and `contour` — take an isovalue and a bounding box, and MUST expose their sampling resolution as an ordinary resolution argument (§8).
'''
enforced_by = [
    "tests/test_isosurface.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-18"
aliases = ["S-18"]
layer = "contract"
section = "6.5 Meshes and surfaces"
statement = '''
Fillets and textured tiles (`fillet`, `interior_fillet`, `textured_tile`) and the `ruler` annotation are part of the surface toolkit.
'''
enforced_by = [
    "tests/test_validation_messages.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-19"
aliases = ["S-19"]
layer = "contract"
section = "6.6 Sweeps, skins and extrusions"
keyword = "MUST"
statement = '''
The sweep family MUST cover BOSL2's: `path_sweep`, `path_sweep2d`, `linear_sweep`, `rotate_sweep`, `spiral_sweep`, `offset_sweep`, generic `sweep`, and `skin` across profiles.
'''
enforced_by = [
    "tests/test_skin.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-19a"
aliases = ["S-19a"]
layer = "contract"
section = "6.6 Sweeps, skins and extrusions"
title = "A sweep returns a solid."
keyword = "MUST NOT"
statement = '''
Every member of the sweep family returns a `Solid`, so its result composes with `-`/`|`/`&` and the transforms like any other shape. A caller writing `path.path_sweep(profile) - hole` MUST NOT have to know that a mesh was the intermediate, and MUST NOT have to append `.polyhedron()` to a call that already said "sweep this into a solid". The mesh stays reachable — `.vnf()` on the result, for measuring, joining or exporting without a CAD runtime (C-8) — but it is the detail, not the return value.
'''
enforced_by = [
    "tests/test_sweep_contract.py",
]
candidates = [
    "tests/test_beziers.py::test_bezier_sweep_valid",
    "tests/test_beziers.py::test_sweep_valid",
    "tests/test_skin.py::test_oop_skin_and_sweep",
    "tests/test_skin.py::test_path_sweep_arrow_cap",
    "tests/test_sweep_contract.py::test_every_sweep_keeps_its_mesh_reachable",
    "tests/test_sweep_contract.py::test_every_sweep_returns_a_solid",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-19b"
aliases = ["S-19b"]
layer = "contract"
section = "6.6 Sweeps, skins and extrusions"
title = "A return type is one type."
keyword = "MAY"
statement = '''
No public callable MAY return a union whose arm is selected by one of its own boolean parameters. `path_sweep(..., transforms=True)` returning a list of matrices instead of geometry is the pattern this forbids: it makes the union unavoidable, so every caller of the *ordinary* case pays for the flag with a narrowing they cannot skip, and the documented one-liner stops type-checking. A flag that changes the return type is a second function — name it (`path_sweep_transforms()`), and let each return one thing.
'''
enforced_by = [
    "tests/test_sweep_contract.py",
]
candidates = [
    "tests/test_sweep_contract.py::test_no_sweep_signature_carries_a_union_return",
    "tests/test_sweep_contract.py::test_the_transforms_flag_is_its_own_function",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-19c"
aliases = ["S-19c"]
layer = "contract"
section = "6.6 Sweeps, skins and extrusions"
title = "Sweeps agree on winding."
statement = '''
Every mesh the sweep family produces has outward-facing normals and therefore positive `volume()`. Two members of one family disagreeing on winding is not a stylistic difference: a mesh handed to `polyhedron()` inside-out exports fine on its own and then *adds* material where it was meant to cut. Whatever guards this for one member guards it for all of them.
'''
enforced_by = [
    "tests/test_sweep_contract.py",
]
candidates = [
    "tests/test_sweep_contract.py::test_every_sweep_winds_outward",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-20"
aliases = ["S-20"]
layer = "contract"
section = "6.6 Sweeps, skins and extrusions"
statement = '''
Sweeps take their cross-section orientation method, sampling type, and skin vertex-matching method as enums (`SweepMethod`, `SamplingType`, `SkinMethod`), never as strings.
'''
enforced_by = [
    "tests/test_exports.py",
]
candidates = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-21"
aliases = ["S-21"]
layer = "contract"
section = "6.6 Sweeps, skins and extrusions"
statement = '''
Offset-sweep end treatments are objects (`os_circle`, `os_smooth`, `os_teardrop`, `os_chamfer`, `os_flat`, `os_profile`), so a rim treatment is passed as one value rather than several loose parameters.
'''
enforced_by = [
    "tests/test_exports.py",
]
candidates = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-22"
aliases = ["S-22"]
layer = "contract"
section = "6.6 Sweeps, skins and extrusions"
statement = '''
Other extrusions — `extrude_from_to`, `cylindrical_extrude`, `text3d`, `path_text`, `chain_hull`, `minkowski_difference` — are part of the specified surface.
'''
enforced_by = [
    "tests/test_texture_application.py::TestText3d::test_it_builds_text_of_the_requested_size",
]
status = "enforced"
note = '''
`text3d` was the one extrusion the rule names that nothing exercised.
'''

[[requirement]]
id = "SPEC-S-23"
aliases = ["S-23"]
layer = "contract"
section = "6.7 Strokes and end caps"
keyword = "MUST"
statement = '''
Any path MUST be strokeable: `stroke(width, closed, endcaps, endcap1, endcap2, joints)` yields a filled outline in 2-D and a solid tube in 3-D, and `dashed_stroke` yields the dashed form. Both are also part of the backend contract, so a stroke works on either backend.
'''
enforced_by = [
    "tests/test_regions.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-23a"
aliases = ["S-23a"]
layer = "contract"
section = "6.7 Strokes and end caps"
title = "One family, one kind of answer."
statement = '''
A stroke is the **area the pen covers**, so in 2-D it is a `Region` — outlines-with-holes, which is what a dashed stroke needs and what a stroked region already returned — and in 3-D a `Solid`. `Path2D.stroke()` returning a `Path2D` described the *boundary* rather than the mark, and left one member of a four-member family answering a different kind of thing from the other three (`Path2D.dashed_stroke`, `Region.stroke`, `Region.dashed_stroke` all return `Region`; `Path3D.stroke` returns `Solid`). The underlying outline stays reachable through the region's `paths`.
'''
enforced_by = [
    "tests/test_rounding.py",
]
candidates = [
    "tests/test_backend_matrix.py::test_stroke_of_a_2d_path_is_a_backend_free_outline",
    "tests/test_drawing.py::test_stroke_2d_builds",
    "tests/turtle/test_turtle2d.py::test_turtle_stroke",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-24"
aliases = ["S-24"]
layer = "contract"
section = "6.7 Strokes and end caps"
keyword = "MUST"
statement = '''
End caps and joints are described by one type — `CapType` for the named styles (butt, round, sphere, circle, arrow variants, diamond, dot, tail, custom) and `CapSpec` for a parameterised or custom-path cap. A cap MUST be expressible as a single value passed to `endcap1=`/`endcap2=`/`joints=`.
'''
enforced_by = [
    "tests/test_drawing.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-25"
aliases = ["S-25"]
layer = "contract"
section = "6.7 Strokes and end caps"
keyword = "MUST"
statement = '''
Stroke width, cap geometry and joint geometry MUST scale together, and a decorative cap MUST trim the body so the finished stroke keeps the requested end position.
'''
enforced_by = []
status = "reviewed"
note = '''
That a decorative cap trims the body so the stroke keeps its requested end position is checked by eye in the render tests; nothing asserts the end position numerically.
'''

[[requirement]]
id = "SPEC-S-26"
aliases = ["S-26"]
layer = "contract"
section = "6.8 Masks, profiles and edge treatments"
statement = '''
2-D mask profiles (`mask2d_roundover`, `mask2d_chamfer`, `mask2d_cove`, `mask2d_tear`, `mask2d_step`, `mask2d_groove`, …) and 3-D masks (`mask3d_roundover`, `mask3d_chamfer`, `mask3d_groove`) are first-class shapes, usable on their own or through a solid's `edge_mask`/`edge_profile`/`edge_profile_asym`/`corner_profile`/`face_profile`.
'''
enforced_by = [
    "tests/test_contract_typing.py",
]
candidates = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-27"
aliases = ["S-27"]
layer = "contract"
section = "6.8 Masks, profiles and edge treatments"
keyword = "MUST"
statement = '''
Which edges or corners a treatment applies to MUST be expressed in the anchor language (C-10), including the `edges=`/`except_edges=` selectors and the axis presets.
'''
enforced_by = [
    "tests/test_masking.py",
]
status = "enforced"
note = '''
Edge and corner selection goes through the anchor language, exercised across the mask surface.
'''

[[requirement]]
id = "SPEC-S-26a"
aliases = ["S-26a"]
layer = "contract"
section = "6.8 Masks, profiles and edge treatments"
title = "A mask does not ask for what its parent knows."
keyword = "MUST"
statement = '''
A 3-D mask cuts a solid, and the solid already knows its own box, so `size=` MUST be optional and derived from the parent when the mask is applied through `edge_mask`/`edge_profile`/`corner_profile`/`face_profile` (P-3, D-6). `mask3d_roundover(r, size)` demanding two positional arguments — one of which is the parent's dimensions — is P-1 inverted on the treatment users reach for most.
'''
enforced_by = [
    "tests/test_mask_contract.py",
]
candidates = [
    "tests/test_mask_contract.py::test_a_groove_derives_its_depth_and_length",
    "tests/test_mask_contract.py::test_a_named_treatment_needs_only_the_treatment",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-26b"
aliases = ["S-26b"]
layer = "contract"
section = "6.8 Masks, profiles and edge treatments"
title = "Masks are applied by name, not by children."
statement = '''
The edge treatments take the *treatment*, not a native handle: `solid.round_edges(Anchor.TOP, radius=3)` and `solid.chamfer_edges(edges, chamfer=2)` are the spellings a caller should reach for, with `edge_mask(edges, mask)` remaining for a custom `Mask3D`. A parameter named `children` typed as a raw native object is OpenSCAD's module-with-children vocabulary surviving into a Python API, which B2-2 exists to replace, and it leaks an L2 type through an L3 signature (A-1).
'''
enforced_by = [
    "tests/test_mask_contract.py",
]
candidates = [
    "tests/test_mask_contract.py::test_a_named_treatment_needs_only_the_treatment",
    "tests/test_mask_contract.py::test_no_mask_factory_has_more_than_one_required_argument",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-26c"
aliases = ["S-26c"]
layer = "contract"
section = "6.8 Masks, profiles and edge treatments"
title = "Mask parameters follow the same rules as everything else."
statement = '''
A mask names its size measure the way the rest of the library does — `radius=`/`diameter=` through `pick_radius()` (D-5), not `r`; `width`/`depth`/`length` rather than `x`/`y` — and takes at most one required argument (D-2). `mask3d_groove(width, depth, length)` with three required positionals is the case D-2 says is never acceptable.
'''
enforced_by = [
    "tests/test_mask_contract.py",
]
candidates = [
    "tests/test_mask_contract.py::test_a_mask_names_its_size_measure_the_way_everything_else_does",
    "tests/test_mask_contract.py::test_no_public_masking_signature_mentions_a_native_type",
    "tests/test_mask_contract.py::test_the_chamfer_profile_names_what_it_measures",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-26d"
aliases = ["S-26d"]
layer = "contract"
section = "6.8 Masks, profiles and edge treatments"
title = "A mask profile is a shape."
statement = '''
~~2-D masks return a `Flat`, not a `Path2D`.~~ **Withdrawn** (§13 rule 5). Implementation showed it makes the primary use worse and breaks the layering: a 2-D mask exists to be *swept along an edge*, which needs a path, and the `Mask2D` factories are pure L0 — they build a `Path2D` out of arithmetic with no CAD runtime in sight, whereas `Path2D.polygon()` crosses the FFI. Returning a `Flat` would force every mask construction through a native runtime that A-2 says L0 must not need, to spare a `.polygon()` call in the rarer case. A mask profile is data; `.polygon()` is the one step to geometry when a caller wants it. S-26's "first-class" stands — it means the profiles are named, reusable values rather than inline point lists, which they are.
'''
enforced_by = []
status = "withdrawn"
note = '''
Withdrawn in the prose; see the statement.
'''

[[requirement]]
id = "SPEC-S-28"
aliases = ["S-28"]
layer = "contract"
section = "6.8 Masks, profiles and edge treatments"
keyword = "MUST"
statement = '''
Rounding and chamfering MUST be available both as constructor parameters (`rounding=`, `chamfer=`, and their per-end variants) and as masks, and the two MUST agree geometrically.
'''
enforced_by = []
status = "reviewed"
note = '''
Both spellings exist and are tested separately; that they *agree geometrically* is asserted nowhere. Comparing a rounded constructor against the equivalent mask is the missing test.
'''

[[requirement]]
id = "SPEC-S-29"
aliases = ["S-29"]
layer = "contract"
section = "6.9 Partitions and joinery"
keyword = "MUST"
statement = '''
A solid MUST be splittable for printing: `partition`, `partition_mask`, `partition_cut_mask`, `partition_path`, and the half-cuts (`half_of`, `left_half`, `top_half`, …).
'''
enforced_by = [
    "tests/test_partitions.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-30"
aliases = ["S-30"]
layer = "contract"
section = "6.9 Partitions and joinery"
statement = '''
The cut profile is an enum (`PartitionCutType`: flat, sawtooth, sinewave, comb, finger, dovetail, hammerhead, jigsaw, square, triangle, halfsine, semicircle), with spread, gap and slop as defaulted parameters.
'''
enforced_by = [
    "tests/test_exports.py",
]
candidates = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-31"
aliases = ["S-31"]
layer = "contract"
section = "6.10 Distribution and copies"
keyword = "MUST"
statement = '''
Copying is available in two forms that MUST stay in step: methods on a shape (`Distributable`: `line_copies`, `xcopies`/`ycopies`/`zcopies`, `grid_copies`, `rot_copies` and its axis variants, `arc_copies`, `sphere_copies`, `path_copies`, `mirror_copy`, flips) and the matrix-producing form (`DistributableMatrix`) for callers that want the transforms themselves.
'''
enforced_by = [
    "tests/test_distributors.py",
]
candidates = [
    "tests/test_sdf_flat_parity.py::test_colour_does_not_leak_between_copies",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-32"
aliases = ["S-32"]
layer = "contract"
section = "6.10 Distribution and copies"
statement = '''
`xdistribute`/`ydistribute`/`zdistribute` spread a set of shapes along an axis by their own sizes; `distribute_on_path` places copies along a path with correct orientation.
'''
enforced_by = [
    "tests/test_distributors.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-33"
aliases = ["S-33"]
layer = "contract"
section = "6.10 Distribution and copies"
keyword = "MUST"
statement = '''
Distribution MUST work on paths and 2-D shapes as well as solids.
'''
enforced_by = [
    "tests/test_distributors.py",
]
status = "enforced"
note = '''
Distribution is exercised on paths and 2-D shapes as well as solids.
'''

[[requirement]]
id = "SPEC-S-34"
aliases = ["S-34"]
layer = "contract"
section = "6.11 Textures"
statement = '''
Named textures (`texture("diamonds")`, ribs, bricks, pyramids, hills, rough, …) come from one registry, and each is either a height field or a VNF tile — the caller does not need to know which.
'''
enforced_by = [
    "tests/test_documented_arguments.py",
]
candidates = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-35"
aliases = ["S-35"]
layer = "contract"
section = "6.11 Textures"
keyword = "MUST"
statement = '''
Anything that can be textured MUST accept the texture by name or object plus the same set of controls: tile size or repeat count, depth, inset, rotation, and per-texture options.
'''
enforced_by = [
    "tests/test_texture_application.py::test_the_texture_reaches_the_geometry",
]
status = "enforced"
note = '''
Every callable that declares `texture=` is built with and without one and required to differ, and the scan is over the package rather than over the case list, so a new declarer must add itself. That is what would have caught the bottle caps, which accepted a style and built a plain wall for as long as they existed -- with a module comment saying so, which makes it a documented silent wrong answer rather than an excuse for one (E-5).
'''

[[requirement]]
id = "SPEC-S-36"
aliases = ["S-36"]
layer = "contract"
section = "6.12 Colour and display modifiers"
statement = '''
`Color` normalises every input form — CSS name, `#rrggbb`, RGB/RGBA sequences in 0–1 or 0–255 — into one RGBA value, and exposes `rgb`, `rgba`, `alpha`, `hex`.
'''
enforced_by = [
    "tests/test_color.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-37"
aliases = ["S-37"]
layer = "contract"
section = "6.12 Colour and display modifiers"
statement = '''
Colour is applied through chainable methods on anything drawable: `color`, `recolor` (recursive), `color_this` (this shape only), plus `hsl`/`hsv` construction. Opacity travels with the colour.
'''
enforced_by = [
    "tests/test_sdf_flat_parity.py",
]
candidates = [
    "tests/test_sdf_flat_parity.py::test_the_sdf_flat_shape_satisfies_the_contract",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-38"
aliases = ["S-38"]
layer = "contract"
section = "6.12 Colour and display modifiers"
statement = '''
The preview modifiers `highlight` (`#`) and `ghost` (`%`) are methods on the same contract, so a debugging aid is a chained call rather than a different API.
'''
enforced_by = [
    "tests/test_backend_parity.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-39"
aliases = ["S-39"]
layer = "contract"
section = "6.12 Colour and display modifiers"
statement = '''
`rainbow` / `rainbow_colors` colour a sequence of objects around the hue wheel.
'''
enforced_by = [
    "tests/test_color.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-40"
aliases = ["S-40"]
layer = "contract"
section = "6.12 Colour and display modifiers"
keyword = "MUST"
statement = '''
Colour MUST be available on paths and regions as well as solids, and MUST survive the conversions between them where the target can carry it.
'''
enforced_by = [
    "tests/test_color.py",
]
candidates = [
    "tests/test_sdf_flat_parity.py::test_colour_survives_the_extrusion_into_three_dimensions",
    "tests/test_sdf_flat_parity.py::test_the_sdf_flat_shape_satisfies_the_contract",
]
status = "enforced"
note = '''
Colour on paths and regions, and its survival through the conversions, are both exercised.
'''

[[requirement]]
id = "SPEC-S-41"
aliases = ["S-41"]
layer = "contract"
section = "6.13 Import, export and interchange"
statement = '''
SVG import produces ordinary geometry: outlines (`svg_outlines`), a `Region` (`region_from_svg`), grouped elements (`svg_element_groups`), and colour-carrying rings (`svg_rings_with_colors`) — with curve flattening controlled by the ordinary resolution arguments (§8).
'''
enforced_by = [
    "tests/test_svg.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-42"
aliases = ["S-42"]
layer = "contract"
section = "6.13 Import, export and interchange"
statement = '''
Mesh import (`osimport`, 2-D and 3-D) returns a wrapped shape that joins the fluent API, not a raw native handle.
'''
enforced_by = [
    "tests/test_shapes2d_object.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-43"
aliases = ["S-43"]
layer = "contract"
section = "6.13 Import, export and interchange"
statement = '''
`polyhedron()` and `VNF.polyhedron()` are the mesh-to-solid boundary; `VNF` is the interchange type for anything that wants to inspect or measure geometry without a CAD runtime.
'''
enforced_by = [
    "tests/test_sweep_contract.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-53"
aliases = ["S-53"]
layer = "contract"
section = "6.13 Import, export and interchange"
title = "There is a way out of the library."
keyword = "MUST"
statement = '''
A caller who has built something MUST be able to write it to a file: `shape.export(path)` on any shape, `VNF.export(path)` on any mesh, with the format taken from the suffix and overridable. This section is titled "import, export and interchange" and specified only import; a modelling toolkit whose output can be *displayed* but not *saved* is not finished, and "drive the app yourself" is not an answer for a library whose L0 promise is that geometry works with no CAD runtime present (A-2).
'''
enforced_by = [
    "tests/test_sweep_contract.py",
]
candidates = [
    "tests/test_export.py",
    "tests/test_no_mesher.py::test_exporting_surfaces_the_real_error",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-54"
aliases = ["S-54"]
layer = "contract"
section = "6.13 Import, export and interchange"
title = "Export is pure where it can be."
keyword = "MUST"
statement = '''
Mesh formats that are pure data — STL (binary and ASCII), OBJ, OFF, PLY — MUST be written by pybosl2 itself from a `VNF`, with no native runtime involved, so exporting works everywhere `import pybosl2` does. A solid exports by meshing to a `VNF` first; where a backend can only mesh through its runtime, the requirement is that runtime, not a hand-rolled second mesher. Formats that are a CAD kernel's own (3MF, AMF, DXF, SVG out) MAY delegate, and MUST raise naming what is missing rather than writing a broken file.
'''
enforced_by = [
    "tests/test_sweep_contract.py",
]
candidates = [
    "tests/test_export.py::test_a_kernel_format_is_refused_by_name",
    "tests/test_export.py::test_a_solid_built_any_other_way_exports_too",
    "tests/test_export.py::test_writing_a_mesh_touches_no_native_module",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-55"
aliases = ["S-55"]
layer = "contract"
section = "6.13 Import, export and interchange"
title = "Export refuses to write nonsense."
statement = '''
A mesh that is not watertight, or is wound inside-out, is a bug the caller wants to hear about before a slicer tells them. `export()` validates (`VNF.is_watertight`, positive `volume()`) and raises naming the defect, with an explicit `check=False` for the caller who knows their surface is open on purpose.
'''
enforced_by = [
    "tests/test_stl_render.py",
]
candidates = [
    "tests/test_export.py::test_an_inside_out_mesh_is_refused",
    "tests/test_export.py::test_writing_a_mesh_touches_no_native_module",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-44"
aliases = ["S-44"]
layer = "contract"
section = "6.14 Parts library"
statement = '''
Every part is a class per C-14, driven by a catalogue name or nominal size (P-4).
'''
enforced_by = [
    "tests/test_exports.py::test_every_part_exposes_shape_as_a_property",
]
status = "enforced"
note = '''
Every part is walked and required to be a class with a `shape` property, which is C-14's half of this.
'''

[[requirement]]
id = "SPEC-S-45"
aliases = ["S-45"]
layer = "contract"
section = "6.14 Parts library"
keyword = "MUST"
statement = '''
The library MUST cover the BOSL2 parts set: gears (spur, helical, herringbone, rack, ring, bevel, worm), screws/nuts/holes and threading (ISO, trapezoidal, acme, square, buttress), drive recesses (Phillips, hex, Torx, Robertson), ball and linear bearings, hinges and snap connectors, dovetails and snap pins, cube truss, modular hose, NEMA steppers, bottle caps, sliders and rails, tripod plates, Platonic solids, FDM walls, wiring bundles, hooks.
'''
enforced_by = [
    "tests/test_bosl2_coverage.py",
]
status = "enforced"
note = '''
The parts set is measured against upstream BOSL2 rather than asserted, which is what B2-1 asks of a coverage claim.
'''

[[requirement]]
id = "SPEC-S-46"
aliases = ["S-46"]
layer = "contract"
section = "6.14 Parts library"
keyword = "MUST"
statement = '''
Parts MUST expose their catalogue as data (spec objects and tables), so a caller can query dimensions — and the docs can tabulate them — without building geometry.
'''
enforced_by = []
status = "reviewed"
note = '''
The spec objects and tables exist and the parts tests read dimensions off them, but nothing requires a *new* part to expose its catalogue as data.
'''

[[requirement]]
id = "SPEC-S-46a"
aliases = ["S-46a"]
layer = "contract"
section = "6.14 Parts library"
title = "Parts honour the active backend."
statement = '''
A part builds through the backend-neutral façade (L3), not by importing a backend's module directly, so `with use_backend("sdf"): Screw(…)` produces an SDF part. Where a part needs an operation only one backend can express, it raises `UnsupportedByBackendError` naming that backend (B-4) rather than quietly returning a shape from the other one. Parts are not exempt from PAR-1.
'''
enforced_by = [
    "tests/test_parts_backend_coverage.py",
]
candidates = [
    "tests/test_part_show.py::test_every_part_refuses_on_another_backend",
    "tests/test_part_show.py::test_the_converted_list_matches_what_actually_builds",
    "tests/test_parts_backend_coverage.py",
]
status = "enforced"
note = '''
Every part is built under both backends and required to build or refuse, which is the rule.
'''

[[requirement]]
id = "SPEC-S-47"
aliases = ["S-47"]
layer = "contract"
section = "6.15 Documentation surface"
keyword = "MUST"
statement = '''
Every subsystem above MUST appear in the generated API reference under a `DocCategory`, and every geometry-producing callable MUST carry a rendering example (§10).
'''
enforced_by = [
    "tests/test_docs_links.py",
]
status = "enforced"
note = '''
Every public module needs a DocCategory and a reference page; the docs tests walk them.
'''

[[requirement]]
id = "SPEC-S-48"
aliases = ["S-48"]
layer = "contract"
section = "6.16 Display and output"
title = "`show()` is how a shape leaves the library."
statement = '''
It hands the shape to the renderer: inside the PythonSCAD app it displays in the viewer, and in a script it marks the shape as what the run produces. It is the one call in the library with a side effect on the session, and the convention every docstring example ends with.
'''
enforced_by = []
status = "reviewed"
note = '''
`show()` exists on every contract and returns the shape (S-49, enforced). That it *hands the shape to the renderer* cannot be asserted without the app, which the render tests need and skip without.
'''

[[requirement]]
id = "SPEC-S-49"
aliases = ["S-49"]
layer = "contract"
section = "6.16 Display and output"
title = "`show()` returns the shape."
keyword = "MUST NOT"
statement = '''
It closes a chain without swallowing the value — `part = cuboid([20, 20, 10]).up(5).show()` both displays and binds. It MUST NOT return `None`.
'''
enforced_by = [
    "tests/test_exports.py::test_part_show_returns_the_shape",
]
candidates = [
    "tests/test_exports.py::test_part_show_returns_the_shape",
    "tests/test_part_show.py::test_show_returns_the_parts_shape",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-50"
aliases = ["S-50"]
layer = "contract"
section = "6.16 Display and output"
title = "`show()` is on the shared contract."
statement = '''
Every shape can be shown, 2-D or 3-D, on either backend (C-15). Showing an SDF solid meshes its field — that is not the implicit conversion B-5 forbids, because rendering *is* meshing and nothing meshed is handed back. Showing a 2-D distance field refuses instead, naming the extrusion that would make it renderable: a field over the plane has no rendering of its own.
'''
enforced_by = [
    "tests/test_shape_contract.py::test_the_object_has_everything_the_contract_declares",
]
candidates = [
    "tests/test_backend_parity.py::test_realizing_an_sdf_shape_applies_its_appearance",
    "tests/test_backend_parity.py::test_two_dimensional_fields_refuse_to_render",
]
status = "enforced"
note = '''
`show()` is on the shared contract and every implementation is required to have it.
'''

[[requirement]]
id = "SPEC-S-51"
aliases = ["S-51"]
layer = "contract"
section = "6.16 Display and output"
title = "Builders delegate."
statement = '''
A part or any other builder exposes `show()` as a convenience that shows its `shape`, so a caller never has to reach inside to display something.
'''
enforced_by = [
    "tests/test_backend_parity.py",
]
candidates = [
    "tests/test_exports.py::test_part_show_returns_the_shape",
    "tests/test_part_show.py::test_show_returns_the_parts_shape",
]
status = "enforced"

[[requirement]]
id = "SPEC-S-52"
aliases = ["S-52"]
layer = "contract"
section = "6.16 Display and output"
title = "`show()` needs a renderer."
statement = '''
With the pip wheel alone it marks the object for output; the PythonSCAD **app** is what puts it on screen. Documentation examples therefore end in `.show()` and are rendered by the docs build, not by the test suite.
'''
enforced_by = []
status = "reviewed"
note = '''
A statement about what the pip wheel versus the app can do. The render tests skip without a binary by design (PLAN X-2), so nothing asserts it and nothing can.
'''

[[requirement]]
id = "SPEC-B-1"
aliases = ["B-1"]
layer = "contract"
section = "7. Backend model"
statement = '''
The active backend is block-scoped and thread-safe, default `"csg"`; `use_backend("sdf")` switches it for a block, `set_default_backend()` globally.
'''
enforced_by = [
    "tests/test_backend.py",
]
status = "enforced"
note = '''
Block scoping, nesting and the default are exercised; thread-safety follows from the ContextVar the test uses.
'''

[[requirement]]
id = "SPEC-B-2"
aliases = ["B-2"]
layer = "contract"
section = "7. Backend model"
statement = '''
A backend implements a small contract: hand back the constructor for a named shape, build it, do n-ary booleans, extrude paths, and stroke a path.
'''
enforced_by = [
    "tests/test_backend_matrix.py",
]
status = "enforced"
note = '''
Both backends are driven through the whole contract, which is what would fail if one stopped implementing it.
'''

[[requirement]]
id = "SPEC-B-3"
aliases = ["B-3"]
layer = "contract"
section = "7. Backend model"
keyword = "MUST NOT"
statement = '''
The **façade owns the default** for every argument both backends understand: it declares that default in its own signature and always forwards it, so an identical call produces identical geometry on either backend (PAR-5). A backend keeps its own defaults only for options exclusive to it. Arguments a backend has no notion of MUST NOT be forwarded to it — that filtering is by *what the backend declares*, not by *what the caller happened to pass*. The filtering applies to **façade-owned defaults only**; a value the caller supplied is subject to B-9 instead.
'''
enforced_by = [
    "tests/test_defaults.py::test_backends_agree_on_the_defaults_they_share",
]
candidates = [
    "tests/test_backend_matrix.py::test_a_facade_default_is_still_filtered_quietly",
    "tests/test_defaults.py::test_backends_agree_on_the_defaults_they_share",
]
status = "enforced"

[[requirement]]
id = "SPEC-B-4"
aliases = ["B-4"]
layer = "contract"
section = "7. Backend model"
keyword = "MUST NOT"
statement = '''
A backend MUST NOT silently approximate a feature it cannot express; it raises `UnsupportedByBackendError` with a hint naming the alternative or the backend that does support it.
'''
enforced_by = [
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
]
candidates = [
    "tests/test_backend_matrix.py::test_2d_shape_constructors_refuse_on_another_backend",
    "tests/test_polyhedron_convexity.py",
    "tests/test_sdf_flat_parity.py::test_the_nominal_box_is_carried_and_is_not_the_measurement",
]
status = "enforced"

[[requirement]]
id = "SPEC-B-5"
aliases = ["B-5"]
layer = "contract"
section = "7. Backend model"
statement = '''
Conversion is one-directional and honest: SDF → CSG is an exact mesh; CSG → SDF is lossy and not offered implicitly.
'''
enforced_by = []
candidates = [
    "tests/test_backend_parity.py::test_sdf_shapes_keep_their_backend_through_moves_and_colour",
    "tests/test_parts_backend_coverage.py::test_each_refusal_names_non_convexity",
    "tests/test_sdf_flat_parity.py::test_a_colour_operation_keeps_the_shape_a_field",
    "tests/test_sdf_shapes3d.py::test_half_of_takes_an_arbitrary_normal",
]
status = "reviewed"
note = '''
Spot-checked where it bites: the parts' SDF refusals cite non-convexity (test_parts_backend_coverage.py) and PyShape2D.fill no longer meshes across (T34). Whether a *new* conversion is honest is a review question.
'''

[[requirement]]
id = "SPEC-B-6"
aliases = ["B-6"]
layer = "contract"
section = "7. Backend model"
statement = '''
Backend-specific extras stay on the backend's own module.
'''
enforced_by = []
candidates = [
    "tests/test_shape_contract.py",
]
status = "reviewed"
note = '''
Which extras belong on a backend's own module rather than the façade is a placement judgement; A-6 catches only the case where a top-level name builds on the wrong backend.
'''

[[requirement]]
id = "SPEC-B-7"
aliases = ["B-7"]
layer = "contract"
section = "7. Backend model"
statement = '''
Adding a façade shape requires: the façade constructor, a matching constructor in both backends (or an explicit refusal), and a row in the backend matrix tests.
'''
enforced_by = [
    "tests/test_backend_matrix.py",
]
status = "enforced"
note = '''
A façade shape without a row here is a shape the matrix does not build, which fails.
'''

[[requirement]]
id = "SPEC-B-8"
aliases = ["B-8"]
layer = "contract"
section = "7. Backend model"
keyword = "MUST"
statement = '''
What a caller gets when they leave an argument out MUST be inspectable, not silent: `effective_defaults(shape)` reports the active backend's real defaults.
'''
enforced_by = [
    "tests/test_defaults.py::test_effective_defaults_reports_the_real_signature",
]
candidates = [
    "tests/test_defaults.py::test_ambient_res_reaches_the_sdf_backend",
]
status = "enforced"

[[requirement]]
id = "SPEC-B-9"
aliases = ["B-9"]
layer = "contract"
section = "7. Backend model"
keyword = "MAY"
statement = '''
The façade exposes the **union** of what the backends can build, not the intersection, and a caller-supplied argument the active backend cannot honour raises `UnsupportedByBackendError` naming the parameter, the backend, and the way forward. Dropping it is forbidden: `regular_prism(radius1=8, radius2=4)` must not quietly return a straight prism on a backend with no taper. This is B-4 applied to arguments rather than whole features, and it is what lets one body of code — the parts library above all — be written once against the façade and fail loudly where a backend falls short, instead of being written twice.
* A parameter that describes *tessellation* (`fn`/`fa`/`fs`, `realign`, `circumscribe`) is not a feature gap on a backend that has no facets: those MAY be accepted and ignored, provided `effective_defaults()` says so.
'''
enforced_by = [
    "tests/test_backend_matrix.py::test_an_explicit_zero_treatment_is_not_a_request",
    "tests/test_backend_matrix.py::test_turning_something_off_is_a_request_the_backend_has_to_honour",
]
candidates = [
    "tests/test_backend_matrix.py::test_an_argument_the_backend_cannot_honour_is_refused_not_dropped",
    "tests/test_backend_matrix.py::test_an_explicit_zero_treatment_is_not_a_request",
    "tests/test_backend_matrix.py::test_no_part_needs_a_shape_argument_the_facade_cannot_carry",
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
    "tests/test_backend_matrix.py::test_tessellation_arguments_are_accepted_and_ignored",
    "tests/test_backend_matrix.py::test_the_facade_exposes_every_shared_constructors_full_surface",
]
status = "enforced"
note = '''
The carve-out for a value that asks for nothing had a hole in it, found in T40. An explicit zero rounding is "no rounding" and refusing it was a false alarm (`RingHook` was turned away from the SDF backend over `prismoid(rounding=0)`) -- but where the façade's default is `True`, passing `False` is the caller turning something **off**, which is a request. `cuboid(trimcorners=False)` leaves the chamfer running past the corners; the SDF backend trims them regardless, and the argument was being dropped in silence. The wrong answer arrived *through* the guard against false alarms rather than around it. `_is_no_op` now takes the façade's default and calls a falsy value nothing only when the default is falsy too. Two parameters in the whole façade have a truthy default a backend lacks, and both of them are this one.

This rule's own worked example also went stale: `test_an_argument_the_backend_cannot_honour_is_refused_not_dropped` used `cube(10, spin=45)` as the CSG-only option until T40 built `spin` on the SDF backend. The example carried an implicit claim -- that `spin` was CSG-only -- which was never true; it was unwritten. What the rule governs is the refusal, not any particular gap.
'''

[[requirement]]
id = "SPEC-PAR-1"
aliases = ["PAR-1"]
layer = "contract"
section = "7.1 Backend parity"
title = "Parity is the default expectation."
keyword = "MUST"
statement = '''
Anything that can be expressed as a signed-distance field MUST be available on the SDF backend, with the same name, the same argument names, and the same meaning as its CSG counterpart. Divergence needs a reason, not a shrug.
'''
enforced_by = [
    "tests/test_backend_matrix.py",
]
candidates = [
    "tests/test_dimension_validation.py",
    "tests/test_parts_backend_coverage.py",
    "tests/test_sdf_flat_parity.py",
]
status = "enforced"
note = '''
The shared surface is built on both backends and required to agree.
'''

[[requirement]]
id = "SPEC-PAR-2"
aliases = ["PAR-2"]
layer = "contract"
section = "7.1 Backend parity"
title = "New shared features land on both."
keyword = "MUST"
statement = '''
A new façade shape, option, or operation MUST be implemented on both backends, or MUST be accompanied by an explicit refusal (B-4) and an entry in the gap list. "CSG only for now" is a tracked decision, never a silent omission.
'''
enforced_by = [
    "tests/test_backend_matrix.py",
]
candidates = [
    "tests/test_parts_backend_coverage.py::test_every_part_either_builds_on_sdf_or_is_listed",
]
status = "enforced"
note = '''
A new shared feature missing from one backend fails the matrix; the exclusive lists are PAR-3's half.
'''

[[requirement]]
id = "SPEC-PAR-3"
aliases = ["PAR-3"]
layer = "contract"
section = "7.1 Backend parity"
title = "The exclusive lists are minimal and justified."
keyword = "MUST"
statement = '''
`CSG_ONLY_FEATURES` and `SDF_ONLY_FEATURES` are the single source of truth for what is exclusive, and each entry MUST carry the reason it cannot cross over. An exclusive feature is **declared and refuses**, not absent: the backend that lacks it implements the method and raises `UnsupportedByBackendError` naming the way forward (B-4). Two reasons. C-13 asks the SDF backend to refuse attachment *explicitly*, and a method that says why is more explicit than a missing name. And since Python 3.12 an `isinstance` check against a runtime-checkable protocol uses static lookup, so a member supplied only by a dynamic fallback makes `isinstance(sdf_solid, Solid)` false for a perfectly good solid — which is how the contract would come to exclude the very shapes it describes. A caller that needs to know *before* calling consults the exclusive list, not the shape. Today that is the attachment/anchor system (CSG-only, because anchoring needs a shape's face and edge structure, which a distance field does not retain), `projection` and `fill` (CSG-only, because a 2-D shadow of a solid and a filled outline are not derivable in closed form from a field — both backends *do* build 2-D shapes otherwise), and the implicit-surface `round`/`chamfer` methods that survive transforms (SDF-only, because CSG expresses those as constructor parameters). An entry that becomes implementable MUST be removed from the list, not left as a permanent excuse; the list is re-reviewed whenever either backend gains a capability.
'''
enforced_by = [
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
]
candidates = [
    "tests/test_backend_capabilities.py::test_csg_attachment_features_unsupported_on_sdf",
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
    "tests/test_error_contract.py::test_hasattr_on_an_unknown_name_answers_rather_than_raising",
    "tests/test_sdf_flat_parity.py::test_the_nominal_box_is_carried_and_is_not_the_measurement",
    "tests/test_sdf_shapes3d.py::test_half_of_takes_an_arbitrary_normal",
    "tests/test_shape_contract.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-PAR-4"
aliases = ["PAR-4"]
layer = "contract"
section = "7.1 Backend parity"
title = "Equivalent options, not just equivalent shapes."
keyword = "MUST"
statement = '''
Parity is measured per *option*: if the CSG cuboid takes `rounding`, `chamfer`, `edges` and `except_edges`, the SDF cuboid MUST take the same ones with the same selector semantics. The **façade** spelling is identical on both backends; where a backend's own module spells a parameter differently, the difference MUST be declared in one central translation table rather than leaking into the façade. The only sanctioned backend-specific control is the resolution knob (`fn`/`fa`/`fs` vs `res`).
'''
enforced_by = [
    "tests/test_option_parity.py::test_no_shape_grows_its_option_gap",
    "tests/test_option_parity.py::test_the_design_note_states_the_measured_gap",
]
candidates = [
    "tests/test_partitions.py::test_partition_returns_two_pieces",
    "tests/test_sdf_flat_parity.py::test_minkowski_refuses_and_names_the_thing_that_does_work",
]
status = "enforced"
note = '''
The two backends' constructors are compared option by option. 176 options the SDF backend lacked were honest parity debt -- refused with the parameter named (B-9) -- and are ratcheted per shape; T40 took that to **113**. The other half has no budget: a *rename* is not a capability gap, and one left out of the translation table surfaces as "the sdf backend cannot do this" when it can. `tube(outer_radius1=8)` built on CSG and refused on SDF, whose `outer_r1` is the same option, and a test asserted that refusal as correct behaviour.

**What T40 closed is the argument for measuring per option rather than per shape.** Every shape in the matrix already built on both backends, so nothing before this rule was broken -- and 63 of the 176 turned out to need no distance-field work at all. 38 were `spin` and `orient`, absent from *every* SDF constructor: a rotation about Z and a rotation of +Z onto a direction, both exact in a field, neither ever written. 11 were `center`, which is not a shape option but `anchor` spelled as a boolean. 14 were an alias declining to pass on what it aliases: `cube` is `cuboid` and `cylinder` and `zcyl` are `cyl` on both backends, but each SDF alias had grown its own field, so `cylinder(rounding=1)` was refused while `cyl(rounding=1)` built it -- the false refusal again, this time caused by a second implementation of one shape rather than a missing table entry (B-3).
'''

[[requirement]]
id = "SPEC-PAR-5"
aliases = ["PAR-5"]
layer = "contract"
section = "7.1 Backend parity"
title = "Same code, same result."
keyword = "MUST"
statement = '''
For any construction both backends support, the two MUST agree on placement, orientation, anchoring, bounds, and units — differing only in tessellation detail. This holds for calls that omit arguments as well as calls that pass them, which is why the façade owns the shared defaults (B-3). The backend matrix tests record what each backend supports, and `docs/design/sdf-csg-compatibility.md` is the working plan for closing the remainder.
'''
enforced_by = [
    "tests/test_backend_parity.py::test_the_same_call_builds_the_same_geometry_on_both_backends",
    "tests/test_option_parity.py::test_an_oblique_cylinder_leans_the_same_way_on_both_backends",
    "tests/test_option_parity.py::test_center_places_the_same_on_both_backends",
    "tests/test_option_parity.py::test_spin_and_orient_place_the_same_on_both_backends",
    "tests/test_option_parity.py::test_the_oblique_shear_is_taken_about_the_mid_plane",
]
candidates = [
    "tests/test_backend_matrix.py",
    "tests/test_backend_parity.py::test_a_cut_that_trims_an_end_tightens_the_bounds",
    "tests/test_backend_parity.py::test_a_private_name_is_answered_without_meshing",
    "tests/test_backend_parity.py::test_the_same_call_builds_the_same_geometry_on_both_backends",
    "tests/test_sdf_shapes3d.py::test_bounds_are_the_wedge_not_the_disc",
    "tests/test_sdf_shapes3d.py::test_reflex_sector",
    "tests/test_sdf_shapes3d.py::test_round3d_runs",
]
status = "enforced"
note = '''
"Same result" is the half of parity that a per-shape matrix cannot see, and giving the SDF backend the options it was missing (T40) is what made the disagreements reachable. Three were found by comparing the two backends on the *same call*, and each had a symmetric case that would have hidden it:

* **`shift` sheared from the wrong plane.** BOSL2's `shift` is the far end's offset *relative to* the near one, and CSG takes the shear about the mid-plane (`x' = x + shift_x * z / length`, on a cylinder spanning `-h/2 .. h/2`). The SDF backend interpolated from the bottom face, so it built the same lean half a shift from where CSG built it. The relative offset -- the thing anyone would check -- was right.
* **`shift` widened the box by the whole shift at both ends**, when each end disc carries its own radius: a 13-wide box reported for a solid 10 wide. A bound rather than the geometry, but a bound is what every caller measuring a shape reads.
* **`_AXIS_LEAN`, which carries `shift` through the rotation that turns a `cyl` into an `xcyl`, had every sign inverted** in its first version. `shift=[3, 3]` on a symmetric cone passes with the signs backwards, so the tests use an asymmetric shift on each axis.

**A bounds comparison could only see two of the three.** An SDF shape's `bounds()` is the box it *declares*, not a measurement of its field, so the wrong shear plane moves the geometry and leaves the reported box alone -- planting that defect again leaves the cross-backend bounds test green. It took a test that asks the field where the material is (`test_the_oblique_shear_is_taken_about_the_mid_plane`, sampling either side of each end disc). That is a general limit on how far the cross-backend comparison can reach on this backend, not a fact about `shift`.
'''

[[requirement]]
id = "SPEC-PAR-6"
aliases = ["PAR-6"]
layer = "contract"
section = "7.1 Backend parity"
title = "Crossing is explicit."
keyword = "MUST NOT"
statement = '''
Mixing operands from two backends raises `CrossBackendError` naming the conversion (B-5); the library MUST NOT convert silently to make a call succeed.
'''
enforced_by = [
    "tests/test_backend_crossbackend.py",
]
status = "enforced"
note = '''
Mixing operands raises CrossBackendError and the message is asserted.
'''

[[requirement]]
id = "SPEC-D-1"
aliases = ["D-1"]
layer = "contract"
section = "8.1 Argument tiers"
keyword = "MUST"
statement = '''
T3–T5 MUST be keyword-only; T2 SHOULD be.
'''
enforced_by = [
    "tests/test_signatures.py::test_no_file_grows_its_positional_tier_parameters",
]
candidates = [
    "tests/test_defaults.py::test_the_facade_owns_the_shared_defaults",
]
status = "enforced"
note = '''
The same scan; D-1 and PLAN T-9a are this rule in three places.
'''

[[requirement]]
id = "SPEC-D-2"
aliases = ["D-2"]
layer = "contract"
section = "8.1 Argument tiers"
statement = '''
One required parameter is the target; a second needs a written justification in the docstring; three is never acceptable. The justified two-argument cases today are `Screw(spec, length)`, `prismoid(size1, size2)` and `regular_prism(sides, …)` — each names two independent dimensions that no default can invent.
'''
enforced_by = [
    "tests/test_signatures.py::test_no_file_grows_its_three_argument_callables",
]
candidates = [
    "tests/test_defaults.py::test_parts_build_from_their_catalogue_name_alone",
    "tests/test_mask_contract.py::test_no_mask_factory_has_more_than_one_required_argument",
    "tests/test_masking_primitives.py::test_mask2d_step_is_square_unless_told_otherwise",
]
status = "enforced"
note = '''
Scoped to callables that return geometry, which is the frame SPEC 8.1 argues in -- `slerp(a, b, t)` is not a constructor with two parameters too many. 8 remain, ratcheted.
'''

[[requirement]]
id = "SPEC-D-3"
aliases = ["D-3"]
layer = "contract"
section = "8.1 Argument tiers"
keyword = "MUST"
statement = '''
Defaults MUST be immutable.
'''
enforced_by = [
    "tests/test_defaults.py::test_no_mutable_defaults_anywhere_in_the_package",
]
candidates = [
    "tests/test_defaults.py::test_the_facade_owns_the_shared_defaults",
]
status = "enforced"

[[requirement]]
id = "SPEC-D-4"
aliases = ["D-4"]
layer = "contract"
section = "8.1 Argument tiers"
statement = '''
`None` means "not supplied, decide for me" — never "off". "Off" is `0`, `False`, or an explicit enum member.
'''
enforced_by = []
status = "reviewed"
note = '''
Whether a `None` means 'decide for me' rather than 'off' is a reading of intent; T-9b is the same rule and the same answer.
'''

[[requirement]]
id = "SPEC-D-5"
aliases = ["D-5"]
layer = "contract"
section = "8.1 Argument tiers"
keyword = "MUST"
statement = '''
Where a dimension has two conventional spellings (`radius`/`diameter`), the API MUST accept both and require neither. Giving both spellings of the **same** dimension is an error, not a silent preference: it raises `ValueError` naming both parameters and their values. Spellings at different levels of specificity are not a conflict — `radius1` legitimately overrides `radius` — and resolve most-specific-first: `(radius1, diameter1) > (radius2, diameter2) > (radius, diameter)`. This is a deliberate departure from BOSL2's `get_radius()`, which silently prefers the radius (B2-2, E-5).
'''
enforced_by = [
    "tests/test_defaults.py::test_a_radius_and_its_own_diameter_together_are_rejected",
]
candidates = [
    "tests/test_defaults.py::test_a_radius_and_its_own_diameter_together_are_rejected",
    "tests/test_defaults.py::test_different_levels_of_specificity_are_not_a_conflict",
    "tests/test_mask_contract.py::test_a_mask_names_its_size_measure_the_way_everything_else_does",
]
status = "enforced"

[[requirement]]
id = "SPEC-D-6"
aliases = ["D-6"]
layer = "contract"
section = "8.1 Argument tiers"
keyword = "MUST"
statement = '''
A derivable parameter MUST default to "not given" and be derived.
'''
enforced_by = []
status = "reviewed"
note = '''
P-3 in the defaults section; whether a parameter *could* be derived is the same domain judgement.
'''

[[requirement]]
id = "SPEC-D-7"
aliases = ["D-7"]
layer = "contract"
section = "8.1 Argument tiers"
keyword = "SHOULD"
statement = '''
A spec argument SHOULD accept the convenient forms — trade name, plain number, or an explicit mapping — and collapse them immediately into one frozen spec object.
'''
enforced_by = []
status = "reviewed"
note = '''
The parts do collapse their spec arguments and are tested doing it, but nothing requires a new spec argument to accept the convenient forms.
'''

[[requirement]]
id = "SPEC-D-8"
aliases = ["D-8"]
layer = "contract"
section = "8.1 Argument tiers"
keyword = "MUST NOT"
statement = '''
A façade MUST NOT present as optional an argument its backend requires.
'''
enforced_by = [
    "tests/test_defaults.py::test_facade_never_defaults_an_argument_its_backend_requires",
]
candidates = [
    "tests/test_defaults.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-G-1"
aliases = ["G-1"]
layer = "contract"
section = "8.1 Argument tiers"
title = "A parameter family that travels together is a group."
keyword = "MUST"
statement = '''
A parameter family that appears on more than three public callables and is always passed as a unit MUST be available as a frozen group object, so it can be built once and reused. `Placement(anchor, spin, orient)` is the case that motivated this: 50 callables take all three, another 31 take two of them, and every one of those restates the same defaults. `CapSpec` (S-24) is the pattern already in the library. A group is a *value*, not a namespace: it is frozen, it composes (`with_`), and it carries its own defaults. **A group reads in every dimension its members mean something in.** A placement is anchor, spin and orient in three dimensions and anchor and spin in two — there is no third axis in the plane to turn a face towards — so one placement serves a 2-D profile and the solid extruded from it, which is the case worth having.
'''
enforced_by = [
    "tests/test_groups.py::TestPlacementInThePlane::test_one_placement_serves_an_outline_and_the_solid_from_it",
]
status = "enforced"

[[requirement]]
id = "SPEC-G-6"
aliases = ["G-6"]
layer = "contract"
section = "8.1 Argument tiers"
title = "A group refuses what a dimension cannot honour."
keyword = "MUST"
statement = '''
Where a group means less in one dimension than another, the members that do not apply MUST be refused rather than dropped. A `Placement` carrying a real `orient` passed to a 2-D constructor raises, naming the way forward, because silently honouring two of its three members is the wrong answer E-5 forbids — the caller asked for an orientation and got a shape without one, with nothing said. The *default* `orient` is not a request: `Placement()` and `Placement(anchor=...)` are dimension-neutral and pass anywhere, which is what keeps one placement usable across a profile and its extrusion (G-1).
'''
enforced_by = [
    "tests/test_groups.py::TestPlacementInThePlane::test_a_placement_that_really_orients_is_refused",
]
status = "enforced"

[[requirement]]
id = "SPEC-G-7"
aliases = ["G-7"]
layer = "contract"
section = "8.1 Argument tiers"
title = "A group makes a conflict unrepresentable, not merely checked."
keyword = "SHOULD"
statement = '''
Where a family's members are mutually exclusive, the group SHOULD model the exclusion in its shape rather than validate it. `rounding` and `chamfer` are one edge treatment, not two independent sizes — an edge is rounded or chamfered, never both — and the library checked that in **six** places with six different wordings, not one of which said what to do instead. `EdgeTreatment` carries one kind and one size, so there is nothing left to disagree; the loose spellings keep the check (G-3), and it is now written once (G-5).
'''
enforced_by = [
    "tests/test_groups.py::TestEdgeTreatment::test_the_conflict_is_unrepresentable_in_the_group",
]
status = "enforced"

[[requirement]]
id = "SPEC-G-9"
aliases = ["G-9"]
layer = "contract"
section = "8.1 Argument tiers"
title = "Groups are resolved where the arguments already are."
keyword = "SHOULD"
statement = '''
A constructor taking several groups SHOULD resolve them in one call, operating on the arguments it is about to forward rather than beside them. The loose members are already listed in that dict, so resolving in place costs a constructor one wrapper; resolving beside it cost thirteen lines of unpacking in every constructor, which is the transcription B-3 exists to reduce rather than add to. `cube` and `cuboid` went from 53 lines to 28 on this change alone.
'''
enforced_by = []
candidates = []
status = "reviewed"
note = '''
Reviewed rather than enforced: no test can tell "resolved in one call" from "resolved in three" without asserting a shape of code, and the thing that actually goes wrong — a group not reaching the geometry — is covered by the per-group tests in tests/test_groups.py.
'''

[[requirement]]
id = "SPEC-G-8"
aliases = ["G-8"]
layer = "contract"
section = "8.1 Argument tiers"
title = "A parameter is a promise, and an unkept one refuses as a library error."
keyword = "MUST"
statement = '''
A parameter in a public signature says the library honours it. Where this port advertises one it does not build, the refusal MUST be a `Bosl2NotImplementedError` — a `Bosl2Error` so `except Bosl2Error` catches it (E-1), a `NotImplementedError` so existing callers are unaffected, and carrying a hint naming what to do meanwhile (E-2). Four public callables raised a bare `NotImplementedError` instead: `cyl(texture=...)`, `cuboid(teardrop=...)`, `CapType.CIRCLE` and `VNF.from_field` with a range. The gaps are listed and the list only shrinks. **`texture=` is the one that matters:** S-34 and S-35 specify textures as a working subsystem, the registry is built, and thirteen public constructors take the five texture parameters — so grouping those five (G-1) would be polish on a promise nothing keeps, which is why the `Texturing` group was not built.
'''
enforced_by = [
    "tests/test_unimplemented.py::test_every_gap_refuses_as_a_library_error",
]
status = "enforced"

[[requirement]]
id = "SPEC-G-2"
aliases = ["G-2"]
layer = "contract"
section = "8.1 Argument tiers"
title = "A group composes and inherits."
keyword = "SHOULD"
statement = '''
A group SHOULD offer `with_()` to derive a variant without mutating the original, and where the family has ambient defaults (§8.3) a way to read them. `None` in a member still means "not given, decide for me" (D-4), never "off".
'''
enforced_by = [
    "tests/test_groups.py::TestPlacement::test_with_returns_a_new_placement",
]
status = "enforced"

[[requirement]]
id = "SPEC-G-3"
aliases = ["G-3"]
layer = "contract"
section = "8.1 Argument tiers"
title = "The loose spellings survive, and conflicting with the group is an error."
keyword = "MUST"
statement = '''
Introducing a group MUST NOT withdraw the loose parameters. `cuboid([60, 40, 12], anchor=Anchor.BOTTOM)` is the common case and reads better than wrapping one value in a group; BOSL2's names are also kept for exactly this reason (B2-3). Supplying a group *and* one of its members MUST raise, naming both — the same rule and the same reasoning as a radius given beside its own diameter (D-5): the call cannot mean two things at once, and silently preferring one of them turns a typo into wrong geometry.
'''
enforced_by = [
    "tests/test_groups.py::TestPlacement::test_a_group_and_its_own_member_together_are_refused",
]
status = "enforced"

[[requirement]]
id = "SPEC-G-4"
aliases = ["G-4"]
layer = "contract"
section = "8.1 Argument tiers"
title = "Resolution's public answer is the ambient default, not a group."
keyword = "SHOULD"
statement = '''
`Facets(fn, fa, fs, res)` exists, but as **plumbing**: it carries the four values down a call chain as one, which is what R-1 asks for and what a forwarding function most often gets wrong. It is not the spelling a caller should reach for, because resolution already has a better public answer — `use_defaults(fn=64)` sets it for a whole block without threading anything through any call (R-4), and a group passed to every call would be strictly worse. Every public callable taking `fn`/`fa`/`fs`/`res` SHOULD say so in its `Args:`, so a reader meets the ambient mechanism at the parameter rather than having to find `pybosl2.defaults`.
'''
enforced_by = [
    "tests/test_ambient_docs.py::test_every_facet_parameter_documents_the_ambient_default",
]
status = "enforced"

[[requirement]]
id = "SPEC-G-5"
aliases = ["G-5"]
layer = "contract"
section = "8.1 Argument tiers"
title = "One resolution rule, in one place."
keyword = "MUST"
statement = '''
"An explicit value beats the ambient one" (R-5) MUST be implemented once. `resolve_facets` and `resolve_res` were two implementations of it — one for the CSG facet controls and one for the SDF resolution — which is two things to keep in step for no reason. Both now go through `Facets.resolved()`.
'''
enforced_by = [
    "tests/test_groups.py::TestFacets::test_both_resolvers_go_through_the_one_rule",
]
status = "enforced"

[[requirement]]
id = "SPEC-R-1"
aliases = ["R-1"]
layer = "contract"
section = "8.2 Curve resolution is universal"
keyword = "MUST"
statement = '''
Every construction that produces a **circle, arc, rounding, chamfer arc, sphere, cylinder, sweep, texture tile, flattened SVG curve, sampled field, or any other tessellated curve** MUST accept the facet controls — `fn`/`fa`/`fs` on the CSG side, `res` on the SDF side — and MUST **pass them through to every sub-construction it builds**. A part that rounds its corners hands its `fn` to the rounding; a gear hands it to its tooth arcs; a mask hands it to its profile; a sweep hands it to its cross-section. Dropping them part-way down is a defect.
'''
enforced_by = [
    "tests/test_facets.py::test_no_new_curved_api_without_facet_controls",
]
candidates = [
    "tests/test_defaults.py",
    "tests/test_partitions.py::test_curved_profiles_follow_the_facet_count",
    "tests/test_regions.py::test_round_corners_follows_the_facet_count",
    "tests/test_tripod_mounts.py::test_facet_controls_are_accepted_and_passed_down",
]
status = "enforced"

[[requirement]]
id = "SPEC-R-1a"
aliases = ["R-1a"]
layer = "contract"
section = "8.2 Curve resolution is universal"
statement = '''
The trigger is *generating points that approximate a curve*, not *taking a radius*. A callable that places or measures geometry from a radius — arc/rot/sphere copy distributors, `polar_to_xy`, circle-tangent geometry — is outside R-1, because nothing it returns is tessellated. When it is genuinely ambiguous, the deciding question is whether a caller could observe a facet count in the output.
'''
enforced_by = [
    "tests/test_facets.py::test_no_new_curved_api_without_facet_controls",
]
candidates = [
    "tests/test_facets.py",
]
status = "enforced"
note = '''
The scope carve-outs are the lists this test reads, each with its reason.
'''

[[requirement]]
id = "SPEC-R-2"
aliases = ["R-2"]
layer = "contract"
section = "8.2 Curve resolution is universal"
keyword = "MUST"
statement = '''
These controls MUST never be required: omitted, they inherit (§8.3).
'''
enforced_by = [
    "tests/test_defaults.py::test_nothing_set_by_default",
]
status = "enforced"
note = '''
A required facet control would fail every bare-call test in the suite.
'''

[[requirement]]
id = "SPEC-R-3"
aliases = ["R-3"]
layer = "contract"
section = "8.2 Curve resolution is universal"
keyword = "MUST NOT"
statement = '''
A backend that cannot honour a control MUST NOT be handed it.
'''
enforced_by = [
    "tests/test_backend_matrix.py",
]
status = "enforced"
note = '''
The tessellation carve-out in refuse_unhonoured is what this exercises.
'''

[[requirement]]
id = "SPEC-R-4"
aliases = ["R-4"]
layer = "contract"
section = "8.3 Ambient defaults"
keyword = "SHOULD"
statement = '''
Resolution SHOULD be settable once for a block rather than threaded through every call: `use_defaults(fn=64)` (block-scoped, nesting, thread-safe) and `set_defaults(...)` (process-wide).
'''
enforced_by = [
    "tests/test_defaults.py::test_block_scoped_and_restored",
]
status = "enforced"

[[requirement]]
id = "SPEC-R-5"
aliases = ["R-5"]
layer = "contract"
section = "8.3 Ambient defaults"
keyword = "MUST"
statement = '''
An explicitly passed value always wins over the ambient one, and there MUST be a way to opt out of an ambient value rather than only override it: passing `fn=0` means "ignore any ambient `fn`, use `fa`/`fs`", matching OpenSCAD's own `$fn=0`. P-6 applies to ambient defaults as much as to signature defaults.
'''
enforced_by = [
    "tests/test_defaults.py::test_fn_zero_opts_out_of_an_ambient_fn",
]
candidates = [
    "tests/test_defaults.py::test_fn_zero_opts_out_of_an_ambient_fn",
]
status = "enforced"

[[requirement]]
id = "SPEC-R-6"
aliases = ["R-6"]
layer = "contract"
section = "8.3 Ambient defaults"
statement = '''
Ambient values are resolved at **construction** time, so a shape's smoothness is fixed by where it was built.
'''
enforced_by = [
    "tests/test_defaults.py::test_ambient_fn_changes_generated_geometry",
]
status = "enforced"
note = '''
Resolution at construction time is what makes the built shape differ.
'''

[[requirement]]
id = "SPEC-R-7"
aliases = ["R-7"]
layer = "contract"
section = "8.3 Ambient defaults"
statement = '''
With nothing set anywhere, the renderer's own defaults apply (`$fa=12`, `$fs=2`).
'''
enforced_by = [
    "tests/test_defaults.py::test_nothing_set_by_default",
]
candidates = [
    "tests/test_defaults.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-D-9"
aliases = ["D-9"]
layer = "contract"
section = "8.4 Units and frame"
statement = '''
Lengths are millimetres, angles are degrees, the frame is right-handed Z-up. No API takes a unit argument; `INCH` converts at the call site.
'''
enforced_by = []
status = "reviewed"
note = '''
Units are a convention the whole library shares; PLAN S-4a is the same rule, and a scan for a `units=` parameter would make both enforced.
'''

[[requirement]]
id = "SPEC-D-10"
aliases = ["D-10"]
layer = "contract"
section = "8.4 Units and frame"
statement = '''
Shapes are centred on the origin, oriented `+Z`, unrotated by default.
'''
enforced_by = [
    "tests/test_defaults.py::test_backends_agree_on_the_defaults_they_share",
]
status = "enforced"
note = '''
A bare call's placement is asserted on both backends.
'''

[[requirement]]
id = "SPEC-E-1"
aliases = ["E-1"]
layer = "contract"
section = "9. Error contract"
keyword = "MUST"
statement = '''
All library errors derive from one base error type — and that MUST be true of the validation errors too, which are the overwhelming majority. E-4 requires them to be `ValueError`s and E-1 requires them to be `Bosl2Error`s; those are only compatible if the type raised is **both**. `Bosl2ValueError(Bosl2Error, ValueError)` satisfies each: `except ValueError` keeps working for every caller who already wrote it, and `except Bosl2Error` starts working, which today it does not. A contract a caller cannot use in an `except` clause is not a contract.
'''
enforced_by = [
    "tests/test_error_contract.py::test_the_package_raises_no_bare_value_error",
]
candidates = [
    "tests/test_error_contract.py::test_a_validation_error_is_catchable_as_the_library_family",
    "tests/test_error_contract.py::test_the_package_raises_no_bare_value_error",
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "enforced"

[[requirement]]
id = "SPEC-E-2"
aliases = ["E-2"]
layer = "contract"
section = "9. Error contract"
statement = '''
A refusal names the feature, the backend, and the alternative.
'''
enforced_by = [
    "tests/test_backend_parity.py::test_the_refusal_names_the_explicit_conversion",
]
candidates = [
    "tests/test_backend_capabilities.py::test_csg_attachment_features_unsupported_on_sdf",
    "tests/test_error_contract.py::test_the_refusal_still_teaches_when_you_call_it",
    "tests/test_sdf_flat_parity.py::test_minkowski_refuses_and_names_the_thing_that_does_work",
]
status = "enforced"

[[requirement]]
id = "SPEC-E-3"
aliases = ["E-3"]
layer = "contract"
section = "9. Error contract"
statement = '''
Cross-backend mixing names both operands and the conversion that resolves it.
'''
enforced_by = [
    "tests/test_backend_crossbackend.py",
]
status = "enforced"
note = '''
The message names both operands and the conversion.
'''

[[requirement]]
id = "SPEC-E-4"
aliases = ["E-4"]
layer = "contract"
section = "9. Error contract"
keyword = "MUST NOT"
statement = '''
Argument validation raises `ValueError` naming the offending parameter(s) and the accepted spellings. A public entry point MUST NOT `assert` on user input.
'''
enforced_by = [
    "tests/test_defaults.py::test_no_assert_validates_user_input",
]
candidates = [
    "tests/test_anchor_argument_errors.py",
    "tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain",
    "tests/test_defaults.py::test_no_assert_validates_user_input",
    "tests/test_defaults.py::test_no_assertion_error_is_raised_directly",
    "tests/test_defaults.py::test_no_bare_assert_stands_in_for_validation",
    "tests/test_defaults.py::test_validation_messages_name_the_accepted_spellings",
    "tests/test_dimension_validation.py",
    "tests/test_empty_bounds_contract.py",
    "tests/test_error_contract.py::test_a_validation_error_is_catchable_as_the_library_family",
    "tests/test_export.py::test_exporting_a_shape_says_what_to_call_instead",
    "tests/test_part_show.py::test_a_refusal_names_what_the_part_needs",
    "tests/test_polyhedron_convexity.py::test_a_non_convex_polyhedron_is_refused_on_sdf",
    "tests/test_polyline_parameters.py::test_a_path_annotation_is_backed_by_a_guard",
    "tests/test_shapes3d.py::test_a_cone_tip_cannot_be_rounded_or_chamfered",
    "tests/test_validation_messages.py::test_polygon_prism_names_the_wrapper_for_a_non_path",
    "tests/test_validation_messages.py::test_sweep_and_prism_rejections",
    "tests/test_validation_messages.py::test_two_dimensional_only_bezier_operations_reject_3d",
]
status = "enforced"

[[requirement]]
id = "SPEC-E-5"
aliases = ["E-5"]
layer = "contract"
section = "9. Error contract"
keyword = "MUST"
statement = '''
A call that cannot mean what it says MUST fail loudly rather than build something else. Loudly means *raising*: printing a warning and returning one operand unchanged (`flat - solid`), or returning a degenerate shape with infinite bounds (`regular_ngon()` with no radius), is the silent wrong answer this rule forbids. A minimum-argument test that only asserts "does not raise" does not establish E-5 — the result has to be a shape someone could use.
'''
enforced_by = [
    "tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain",
]
candidates = [
    "tests/test_bounds_contract.py::test_no_bounds_is_ever_non_finite",
    "tests/test_defaults.py::test_a_radius_and_its_own_diameter_together_are_rejected",
    "tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain",
    "tests/test_error_contract.py::test_a_constructor_with_nothing_to_build_from_refuses",
    "tests/test_error_contract.py::test_same_dimension_booleans_are_untouched",
    "tests/test_shapes2d.py::test_regular_ngon_with_no_size_refuses",
]
status = "enforced"

[[requirement]]
id = "SPEC-E-6"
aliases = ["E-6"]
layer = "contract"
section = "9. Error contract"
title = "A refusal is still an attribute error."
keyword = "MUST"
statement = '''
An error raised from `__getattr__` because the backend cannot offer that name MUST also be an `AttributeError`, so `hasattr()`, `getattr(x, n, default)`, `inspect`, `copy`, `pickle` and every REPL completion keep working instead of exploding. `UnsupportedByBackendError` inherits `AttributeError` for this reason; the message is unchanged, and code that probes a capability gets `False` rather than a traceback. Refusals raised from a *call* are unaffected.
'''
enforced_by = [
    "tests/test_error_contract.py::test_hasattr_on_an_unknown_name_answers_rather_than_raising",
]
candidates = [
    "tests/test_error_contract.py::test_hasattr_on_an_unknown_name_answers_rather_than_raising",
    "tests/test_error_contract.py::test_the_package_raises_no_bare_value_error",
    "tests/test_no_mesher.py::test_capability_probes_answer_without_meshing",
]
status = "enforced"

[[requirement]]
id = "SPEC-E-7"
aliases = ["E-7"]
layer = "contract"
section = "9. Error contract"
title = "Dimensional mixing raises."
statement = '''
`flat - solid` raises at runtime, naming the extrusion or projection that resolves it (C-4, C-16). The static error is the first line of defence and the right one, but this library's users drive a CAD app and mostly do not run a type checker, so the contract cannot rest on it alone.
'''
enforced_by = [
    "tests/test_error_contract.py::test_mixing_dimensions_raises_and_names_the_way_across",
]
candidates = [
    "tests/test_error_contract.py::test_mixing_dimensions_raises_and_names_the_way_across",
    "tests/test_error_contract.py::test_unsupported_is_both_bases",
    "tests/test_shape_contract.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-DOC-1"
aliases = ["DOC-1"]
layer = "contract"
section = "10. Documentation and catalogue"
statement = '''
Docs are **generated** from the source: module header tags drive the API reference, and docstring examples drive the rendered figures. An API list is never hand-maintained.
'''
enforced_by = [
    "tests/test_bosl2_coverage.py::test_the_committed_page_matches_the_generator",
]
candidates = [
    "tests/test_docs_pages.py::test_the_generated_index_leads_with_the_front_door",
]
status = "enforced"
note = '''
The generated pages are compared against their generators; T27 added the same check for SPEC/PLAN.
'''

[[requirement]]
id = "SPEC-DOC-1a"
aliases = ["DOC-1a"]
layer = "contract"
section = "10. Documentation and catalogue"
title = "Generation MUST NOT destroy what it did not write."
statement = '''
The generator owns the module stubs and `index.rst`; everything else is prose it is a guest among. A page it does not recognise is *deleted*, and a hand-edit to a generated file is *overwritten* — both silently, because neither raises a warning and a build that removes a page still reports success. So a hand-written page is registered with the generator, a link to it belongs in the generator rather than in its output, and a test — not a convention — is what keeps the register honest.
'''
enforced_by = [
    "tests/test_docs_links.py",
]
status = "enforced"
note = '''
Hand-written pages are registered with the generator and the register is checked.
'''

[[requirement]]
id = "SPEC-DOC-2"
aliases = ["DOC-2"]
layer = "contract"
section = "10. Documentation and catalogue"
statement = '''
Every public module, class, and callable is documented, and every callable that produces geometry carries an example that renders.
'''
enforced_by = [
    "tests/test_docs_links.py::test_every_facade_callable_documents_its_arguments_and_shows_an_example",
]
candidates = [
    "tests/test_docs_links.py::test_every_facade_callable_documents_its_arguments_and_shows_an_example",
    "tests/test_docs_links.py::test_every_facade_example_runs",
    "tests/test_example_claims.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-DOC-3"
aliases = ["DOC-3"]
layer = "contract"
section = "10. Documentation and catalogue"
statement = '''
The visual parts catalogue renders each featured part through the real PythonSCAD binary and reports measured metrics — triangles, volume, bounding box, watertightness. Its caches are committed, because CI has no CAD binary.
'''
enforced_by = []
status = "reviewed"
note = '''
The catalogue's measured metrics need the real binary, which CI has not got. The caches are committed and the render tests skip -- so nothing asserts it in CI by design.
'''

[[requirement]]
id = "SPEC-DOC-4"
aliases = ["DOC-4"]
layer = "contract"
section = "10. Documentation and catalogue"
statement = '''
Mechanics — docstring style, header tag values, build commands — are specified in [PLAN.md §5](PLAN.md).
'''
enforced_by = []
status = "reviewed"
note = '''
A statement about where the mechanics are documented, not about the code.
'''

[[requirement]]
id = "SPEC-DOC-5"
aliases = ["DOC-5"]
layer = "contract"
section = "10. Documentation and catalogue"
title = "The examples are the contract, so they are checked like one."
keyword = "MUST"
statement = '''
Every docstring example MUST pass `mypy --strict` as ordinary user code, because that is exactly what it is: the example is what a caller copies, and an example the project's own type checker rejects is a signature defect the docs found first. `path_sweep`'s documented one-liner failing on a union return (S-19b) is the case this rule exists to catch — the mechanics are [PLAN §5](PLAN.md), and it is a gate (Q-6), not a review habit.
'''
enforced_by = [
    "tests/test_docstring_examples.py::test_no_module_grows_its_untyped_examples",
]
candidates = [
    "tests/test_docstring_examples.py::test_no_module_grows_its_untyped_examples",
    "tests/test_facade_discoverability.py",
]
status = "enforced"

[[requirement]]
id = "SPEC-DOC-6"
aliases = ["DOC-6"]
layer = "contract"
section = "10. Documentation and catalogue"
title = "There is a front door."
keyword = "MUST"
statement = '''
The documentation MUST open with a task-oriented getting-started page that carries a reader from installation to a finished, exported part — build, round, cut, place, measure, save — before the API reference. §3's promise is that a caller who knows what they want to build but nothing about the library can build it; a reference indexed by module is not how anyone learns that, and it is the first thing a new user sees. The page's examples run under the same gates as every other example (DOC-2, DOC-5).
'''
enforced_by = [
    "tests/test_docs_pages.py::test_the_front_door_carries_a_reader_end_to_end",
]
candidates = [
    "tests/test_docs_pages.py::test_the_front_door_carries_a_reader_end_to_end",
    "tests/test_docs_pages.py::test_the_front_door_exists_and_is_protected",
]
status = "enforced"

[[requirement]]
id = "SPEC-Q-1"
aliases = ["Q-1"]
layer = "contract"
section = "11. Quality gates"
statement = '''
The full test suite passes on **every supported interpreter — 3.11, 3.12 and 3.13** — and the pure-geometry tests pass with no CAD runtime present. One interpreter is not enough: the versions differ in behaviour this library depends on (`isinstance` against a Protocol resolves differently before and after 3.12), so a green run on the newest says nothing about the oldest, which is the one CI will run anyway. Mechanics in [PLAN L-1, L-1a](PLAN.md#1-language-baseline).
'''
enforced_by = [
    "tests/test_supported_versions.py",
]
status = "enforced"
note = '''
The interpreter list, the classifiers and CI's matrix are required to agree.
'''

[[requirement]]
id = "SPEC-Q-2"
aliases = ["Q-2"]
layer = "contract"
section = "11. Quality gates"
statement = '''
Strict static type checking passes with zero errors.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
mypy --strict is a gate and the workflows are checked for it.
'''

[[requirement]]
id = "SPEC-Q-3"
aliases = ["Q-3"]
layer = "contract"
section = "11. Quality gates"
statement = '''
Lint and format are clean.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
ruff check and format are gates and pre-commit hooks.
'''

[[requirement]]
id = "SPEC-Q-4"
aliases = ["Q-4"]
layer = "contract"
section = "11. Quality gates"
statement = '''
Every new public callable has a minimum-argument test — the mechanical enforcement of P-1 — and a validated docstring example. For façade shape constructors this is enforced automatically by the contract tests; elsewhere it is a review obligation until the check is generalised (§12.2).
'''
enforced_by = [
    "tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain",
]
status = "enforced"
note = '''
Parametrised over the public shape modules plus a parts probe.
'''

[[requirement]]
id = "SPEC-Q-5"
aliases = ["Q-5"]
layer = "contract"
section = "11. Quality gates"
statement = '''
The contract tests guarding these rules (mutable defaults, façade honesty, stub parity, facet coverage, backend matrix, protocol/object agreement, export completeness) still pass.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
The contract tests run in CI with the rest of the suite.
'''

[[requirement]]
id = "SPEC-Q-7"
aliases = ["Q-7"]
layer = "contract"
section = "11. Quality gates"
statement = '''
The **artifact** passes, not only the source tree. A gate builds the wheel, installs it into a clean virtualenv, imports it, and type-checks a short consumer snippet against it. Q-1…Q-6 all read the working copy, so nothing before this asked what a release actually contains — which is how `py.typed` came to be missing from every wheel this project has published while `mypy --strict` ran clean on every commit. What ships is a separate claim from what builds, and an unmeasured claim is a wish (B2-1).
'''
enforced_by = [
    "tests/test_packaging.py::test_the_marker_is_declared_as_package_data",
]
status = "enforced"
note = '''
Two halves. The cheap half is tests/test_packaging.py, which reads the declarations that decide what the build collects. The full half is the `artifact` job in .github/workflows/tests.yml, which builds, installs into an empty virtualenv and type-checks a consumer snippet — it needs a network and a build, so it runs in CI rather than under pytest.
'''

[[requirement]]
id = "SPEC-Q-6"
aliases = ["Q-6"]
layer = "contract"
section = "11. Quality gates"
statement = '''
Every docstring example type-checks under `mypy --strict` (DOC-5), and every example that states a value in a trailing comment is executed and held to it (PLAN D-P5b). The examples are user code; they are held to what the library asks of user code, and to the truth of what they claim.
'''
enforced_by = [
    "tests/test_docstring_examples.py::test_no_module_grows_its_untyped_examples",
]
candidates = [
    "tests/test_ci_gates.py::test_a_workflow_that_runs_the_suite_installs_the_test_extra",
    "tests/test_ci_gates.py::test_a_workflow_that_runs_the_suite_verifies_the_gate_can_run",
    "tests/test_ci_gates.py::test_mypy_is_declared_in_the_test_extra",
    "tests/test_docstring_examples.py::test_no_module_grows_its_untyped_examples",
]
status = "enforced"

[[requirement]]
id = "PLAN-L-1"
aliases = ["L-1"]
layer = "mechanics"
section = "1. Language baseline"
title = "The supported interpreters are 3.11, 3.12 and 3.13, and all three are tested."
keyword = "MUST"
statement = '''
`enum.StrEnum` and `typing.Self` set the floor at 3.11; `requires-python`, the classifier list and CI's matrix (`.github/workflows/tests.yml`) MUST all agree with it. Adding or dropping a version changes all three in one commit, and `tests/test_supported_versions.py` fails if they drift apart.
'''
enforced_by = [
    "tests/test_supported_versions.py::test_each_supported_version_is_named_in_the_plan",
]
candidates = [
    "tests/test_supported_versions.py::test_each_supported_version_is_named_in_the_plan",
    "tests/test_supported_versions.py::test_requires_python_matches_the_floor",
    "tests/test_supported_versions.py::test_the_classifiers_claim_nothing_untested",
]
status = "enforced"

[[requirement]]
id = "PLAN-L-1a"
aliases = ["L-1a"]
layer = "mechanics"
section = "1. Language baseline"
title = "Testing on one interpreter is not testing."
statement = '''
The differences between them are behavioural, not cosmetic, and this project has already been bitten twice by assuming otherwise:
* `isinstance` against a runtime-checkable Protocol uses `hasattr` on 3.11 and a static lookup from 3.12, which is why a computing property on a contract breaks *only* on the oldest supported version (T-6b and T-6e are the two halves — see T-6e).
* `typing.Protocol.__protocol_attrs__` does not exist before 3.12, so a test built on it passes on a new interpreter and errors on the supported floor.

Both were invisible to a full green run on a newer interpreter. So: before calling a change done, run the suite on **3.11** as well as on whatever you develop against — it is the version most likely to disagree with you, and CI runs it whether you did or not:

```bash
  python3.11 -m venv /tmp/venv311 && /tmp/venv311/bin/pip install -e '.[test]'
  /tmp/venv311/bin/python -m pytest -q
```

A construct that is version-specific by necessity is written to work on the floor, not on the newest — and never reaches for a `typing` internal, which is where both of these came from.
'''
enforced_by = [
    "tests/test_no_mesher.py",
]
candidates = [
    "tests/test_supported_versions.py::test_nothing_uses_a_typing_internal",
]
status = "enforced"
note = '''
The 3.11/3.12 protocol difference is probed with hasattr *and* isinstance so it fails on either interpreter.
'''

[[requirement]]
id = "PLAN-L-2"
aliases = ["L-2"]
layer = "mechanics"
section = "1. Language baseline"
statement = '''
Modern syntax only: `X | Y` unions (never `typing.Union`/`Optional`), built-in generics (`list[str]`, `dict[str, float]`), `from __future__ import annotations` at the top of every module so annotations stay cheap and forward references work.
'''
enforced_by = [
    "tests/test_code_hygiene.py::test_no_legacy_typing_spellings",
]
status = "enforced"
note = '''
Six `Union` aliases were converted to make it pass; five were the same alias copied into five modules, and only one imported the names its own copy referenced.
'''

[[requirement]]
id = "PLAN-L-3"
aliases = ["L-3"]
layer = "mechanics"
section = "1. Language baseline"
statement = '''
Use the standard library rather than re-deriving it: `math`, `itertools`, `functools`, `dataclasses`, `contextlib`, `contextvars`, and NumPy for array maths. A hand-rolled `lerp`, angle-wrap, or matrix multiply that duplicates `math`/NumPy is a defect.
'''
enforced_by = []
status = "reviewed"
note = '''
Whether a helper re-derives something the standard library has is a judgement about what it computes.
'''

[[requirement]]
id = "PLAN-L-4"
aliases = ["L-4"]
layer = "mechanics"
section = "1. Language baseline"
statement = '''
Third-party runtime dependencies are exactly: `numpy`, `typing-extensions`, `webcolors`, `svgelements`, `shapely`. Adding one is an architectural decision, not a convenience.
'''
enforced_by = [
    "tests/test_declared_surface.py::test_the_runtime_dependencies_are_the_ones_the_plan_names",
]
status = "enforced"
note = '''
pyproject and the plan are compared directly; the list has grown twice and both times by hand.
'''

[[requirement]]
id = "PLAN-T-1"
aliases = ["T-1"]
layer = "mechanics"
section = "2. Typing"
title = "Total coverage."
statement = '''
Every function, method, parameter, and return value carries an explicit annotation — including `-> None`.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
`mypy --strict` over the package and the tests is a CI gate; a missing annotation fails it.
'''

[[requirement]]
id = "PLAN-T-2"
aliases = ["T-2"]
layer = "mechanics"
section = "2. Typing"
title = "No implicit `Any`."
statement = '''
`Any` appears only where it is genuinely unavoidable: the PythonSCAD FFI boundary and `**kwargs` passthrough to it. Everywhere else it is a defect. When a value arrives as `Any` from the FFI, `cast()` it to a real type on the first line that touches it, and wrap native callables obtained via `native("…")` in a typed `Callable`/`Protocol` signature.
'''
enforced_by = []
status = "reviewed"
note = '''
`mypy --strict` catches an *implicit* Any, but an explicit one is legal and the rule is about whether it was unavoidable. C-23 ratchets the contract's share of them; the rest is review.
'''

[[requirement]]
id = "PLAN-T-3"
aliases = ["T-3"]
layer = "mechanics"
section = "2. Typing"
title = "Full element types."
statement = '''
A bare `list`, `dict`, `tuple`, or `Sequence` is never acceptable — always `list[float]`, `dict[str, Anchor]`, `tuple[list[float], list[float]]`, `Sequence[Sequence[float]]`. This applies to local variables holding collections, to empty initialisations (`results: list[str] = []`), and to `.pyi` stubs.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
A bare `list`/`dict` annotation is an error under `mypy --strict`'s disallow_any_generics.
'''

[[requirement]]
id = "PLAN-T-4"
aliases = ["T-4"]
layer = "mechanics"
section = "2. Typing"
title = "Inputs widen, outputs narrow — except for points."
statement = '''
Accept `Sequence[float]`, return `list[float]`. **A polyline is the exception** (SPEC C-7a): a public parameter meaning an ordered set of points is typed `Path2D`, `Path3D` or `Path`, never `PathLike` and never a bare sequence or array. Widening is right for a *vector* — three floats are three floats — and wrong for a *path*, because the bare form drops the three things the caller needs the callee to know: dimension, closed-ness, and winding. Every function taking the bare form re-derives them, and they disagree.
'''
enforced_by = [
    "tests/test_exports.py::test_every_polyline_parameter_accepts_a_path",
]
candidates = [
    "tests/test_docstring_examples.py::test_there_are_examples_to_check",
    "tests/test_exports.py::test_every_polyline_parameter_accepts_a_path",
    "tests/test_exports.py::test_no_top_level_name_builds_on_the_wrong_backend",
    "tests/test_polyline_parameters.py::test_clockwise_polygon_returns_an_outline_not_raw_points",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-4a"
aliases = ["T-4a"]
layer = "mechanics"
section = "2. Typing"
title = "`PathLike` is an implementation type."
statement = '''
It types the local normalisation inside a body (`pts = Path2D(x)` at the top), never a parameter in a public signature. Import it for that and nothing else.
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_no_new_parameter_takes_raw_points",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-4b"
aliases = ["T-4b"]
layer = "mechanics"
section = "2. Typing"
title = "Refuse raw points with `require_path()`."
statement = '''
`pybosl2.paths.require_path(value, parameter, function)` returns the `Path` unchanged and otherwise raises `Bosl2ValueError` naming the wrapper to apply — `Path2D(...)` for 2-D points, `Path3D(...)` for 3-D — chosen from the shape of what was passed, so the message is the fix (SPEC C-7b). Call it on the first line, before any other work, so the refusal arrives at the call that was wrong (SPEC E-4).
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_a_path_annotation_is_backed_by_a_guard",
]
candidates = [
    "tests/test_polyline_parameters.py::test_a_path_annotation_is_backed_by_a_guard",
    "tests/test_polyline_parameters.py::test_no_new_parameter_takes_raw_points",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-4b(i)"
aliases = ["T-4b(i)"]
layer = "mechanics"
section = "2. Typing"
title = "Pass `expect=` whenever the annotation names one width."
statement = '''
`require_path(value, parameter, function, Path2D)` checks the concrete type; the bare three-argument form is only for a parameter genuinely typed `Path`, where both widths are meant (`path_tangents`, `total_length`). `Path2D` and `Path3D` are siblings, so the bare form accepts either — which turns a `Path2D` annotation into documentation rather than a check, and lets a 3-D path reach planar arithmetic that silently drops z (SPEC C-7d).
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_a_path_annotation_is_backed_by_a_guard",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-4c"
aliases = ["T-4c"]
layer = "mechanics"
section = "2. Typing"
title = "The migration is ratcheted, not big-banged."
statement = '''
`tests/test_polyline_parameters.py` holds the list of public parameters still accepting a raw sequence; it only ever shrinks, so a new function cannot join them and the remaining debt is a measured number rather than an assertion. Guarded also by `tests/test_exports.py::test_every_polyline_parameter_accepts_a_path`. A ratchet is only as honest as what it can see, so the scan's own scope is part of the rule: it counts `ArrayLike`/`NDArray` as raw (C-7a names a NumPy array explicitly, and leaving numpy's spelling out under-reported the debt by 16), and it counts only module-level functions and the methods of module-level classes — a function nested in a body has no public parameters, and counting one put an entry on the list that no conversion could ever remove.
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_the_list_is_not_stale",
]
candidates = [
    "tests/test_polyline_parameters.py",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-4d"
aliases = ["T-4d"]
layer = "mechanics"
section = "2. Typing"
title = "A normalizer is excluded, not owed."
statement = '''
A function whose *job* is to accept the wide form — `sdf.paths.as_points()`, the SDF layer's equivalent of the `Path2D(...)` constructor — is listed in the scan's `EXCLUDED` set with its reason, never left on `STILL_RAW`. Requiring a `Path` of it would make it a no-op while pushing every internal numpy pipeline through a wrapper, so it is a permanent entry, and a permanent entry on a list defined to only shrink is a lie about the debt.
'''
enforced_by = [
    "tests/test_polyline_parameters.py::test_a_normalizer_is_excluded_rather_than_owed",
]
candidates = [
    "tests/test_polyline_parameters.py::test_a_normalizer_is_excluded_rather_than_owed",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-5"
aliases = ["T-5"]
layer = "mechanics"
section = "2. Typing"
title = "No union-widening in overrides."
keyword = "MUST NOT"
statement = '''
An override MUST NOT broaden a parameter to `float | list[float]` to cover both callers; pick the collection form and convert at the entry point. The one sanctioned union is a *spec argument* at a public constructor (SPEC D-7), which MUST collapse to a single typed object on the first line.
'''
enforced_by = []
status = "reviewed"
note = '''
Whether an override's widened parameter exists 'to cover both callers' is a judgement about why it was written; mypy accepts the widening.
'''

[[requirement]]
id = "PLAN-T-6"
aliases = ["T-6"]
layer = "mechanics"
section = "2. Typing"
title = "Structure with `Protocol`."
statement = '''
Cross-backend contracts (`Shape`, `Flat`, `Solid`, `SolidBackend`) are `Protocol`s with fully typed members — never `Any -> Any` placeholders. Use `@runtime_checkable` only when an `isinstance` check actually exists.
'''
enforced_by = [
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-6a"
aliases = ["T-6a"]
layer = "mechanics"
section = "2. Typing"
title = "The shape protocols are one hierarchy."
statement = '''
SPEC C-15 requires `Shape` with `Flat` and `Solid` derived from it. In Python that is three `Protocol`s in the L1 contract module, with the shared members typed to return `Self`:

```python
  class Shape(Protocol):
      backend: str

      def __or__(self, other: Self) -> Self: ...
      def translate(self, v: Sequence[float]) -> Self: ...
      def bounds(self) -> Bounds2D | Bounds3D: ...


  class Flat(Shape, Protocol):
      def linear_extrude(self, height: float, **kwargs: Any) -> "Solid": ...


  class Solid(Shape, Protocol):
      def projection(self, cut: bool = False) -> Flat: ...
```

`Self` is what makes SPEC C-16 static: `flat | flat` checks, `flat | solid` does not, with no runtime guard and no `TypeVar` machinery. A shared member MUST NOT be re-declared on `Flat` or `Solid` — re-declaring is how the two drifted apart in the first place.
'''
enforced_by = [
    "tests/test_backend_matrix.py::test_one_shape_contract_with_two_specialisations",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-6c"
aliases = ["T-6c"]
layer = "mechanics"
section = "2. Typing"
title = "The protocol declares everything the object does."
statement = '''
SPEC C-20: a public attribute on `CsgSolid`, `SdfSolid`, `CsgShape2D` or `PyShape2D` that no protocol declares is a defect, not an extra. Write the member on `Shape` if both dimensions can honour it (C-22), on `Flat`/`Solid` only if it is genuinely dimensional. Where the two backends' concrete signatures differ in ways the checker cannot reconcile, the protocol declares the **caller's** view and the parameter is typed `Any` with a comment saying which two spellings it is bridging — that is a T-2 exception earned by the contract, and it is bounded by the allowlist in `tests/test_shape_contract.py::test_the_contract_is_the_whole_object`. Prefer widening the protocol to shrinking the object: a method users already call does not stop existing because it is inconvenient to type.
'''
enforced_by = [
    "tests/test_shape_contract.py::test_the_contract_is_the_whole_object",
]
candidates = [
    "tests/test_shape_contract.py",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-6d"
aliases = ["T-6d"]
layer = "mechanics"
section = "2. Typing"
title = "A return type does not depend on a flag."
statement = '''
SPEC S-19b. A `bool` parameter that selects which arm of a union comes back is a second function wearing the first one's name: split it (`path_sweep` / `path_sweep_transforms`). The only unions a public return may carry are ones the *caller's own input* selects — a façade returning `Flat` or `Solid` by dimension — and even those need `@overload` so the checker can narrow. Guarded by `tests/test_exports.py::test_no_public_return_type_is_a_flag_selected_union`.
'''
enforced_by = [
    "tests/test_code_hygiene.py::test_no_public_return_type_is_a_flag_selected_union",
]
status = "enforced"
note = '''
The guard PLAN X-4 has always named and nobody had written. It reads the AST rather than splitting the annotation text, because `list[tuple[str | None, ...]]` is not a union of the return type.
'''

[[requirement]]
id = "PLAN-T-6b"
aliases = ["T-6b"]
layer = "mechanics"
section = "2. Typing"
title = "Protocol members must be real methods."
keyword = "MUST"
statement = '''
Since Python 3.12, `isinstance()` against a `runtime_checkable` Protocol uses static lookup and ignores anything `__getattr__` supplies. A member that exists only through a dynamic passthrough therefore fails the check even though the call works — which is how `show()` was absent from every shape contract while appearing to be present. Anything in a contract MUST be declared on the class.
'''
enforced_by = [
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
]
candidates = [
    "tests/test_backend_capabilities.py::test_csg_attachment_features_unsupported_on_sdf",
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
    "tests/test_shape_contract.py::test_the_object_has_everything_the_contract_declares",
    "tests/test_supported_versions.py",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-6e"
aliases = ["T-6e"]
layer = "mechanics"
section = "2. Typing"
title = "A runtime-checkable Protocol declares no property that does work."
statement = '''
`isinstance()` against such a Protocol calls `hasattr()` on **every** declared member, and `hasattr` on a property *evaluates* it. So a property that meshes, renders, crosses the FFI, or can raise turns a type check into that work — silently, on every call — and, when the work fails, into an exception escaping the check itself, because `hasattr` catches `AttributeError` and nothing else.

`Solid.vnf` was declared as a property and did exactly this: `isinstance(shape, Solid)` meshed an SDF field, and on a machine with no mesher available the resulting `Bosl2ValueError` propagated out of the `isinstance` call. Two backend tests failed with a mesh error from inside a type check.

**This rule and T-6b are two halves of one version change, and the library must satisfy both.** Python 3.11 resolves a protocol `isinstance` with `hasattr`; 3.12 changed it to a static lookup. So on 3.11 a property on a contract is evaluated (this rule) but a member supplied by `__getattr__` is found; on 3.12+ the property is safe but the dynamic member is invisible (T-6b). `requires-python` is `>=3.11`, so code that satisfies only one of the two is broken for some supported interpreter — and, worse, is broken *invisibly* on whichever one the developer happens to run. That is exactly how this reached CI: it is unobservable on 3.12 and later.

`tests/test_no_mesher.py` is the guard, and it probes with `hasattr` directly as well as through `isinstance` so it fails on every interpreter rather than only on 3.11.

So: anything on a contract that computes is a **method** (`bounds()`, `vnf()`, `volume()`, `show()`); a property on a contract is a cheap, total accessor over state the object already holds (`backend`, `size`). This is the same failure family as E-6 — code that probes an object must get an answer, not a traceback — arriving through `isinstance` rather than `getattr`.
'''
enforced_by = [
    "tests/test_shape_contract.py::test_no_contract_member_is_a_property_that_computes",
]
candidates = [
    "tests/test_beziers.py",
    "tests/test_no_mesher.py::test_isinstance_answers_without_meshing",
    "tests/test_shape_contract.py::test_no_contract_member_is_a_property_that_computes",
    "tests/test_skin.py",
    "tests/test_supported_versions.py",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-7"
aliases = ["T-7"]
layer = "mechanics"
section = "2. Typing"
title = "Variance is explicit."
statement = '''
`TypeVar("T", covariant=True)` / `contravariant=True` where a generic is exposed publicly.
'''
enforced_by = []
status = "reviewed"
note = '''
Variance is declared where generics are exposed, and mypy checks the declarations are consistent -- but nothing requires a new public generic to declare it.
'''

[[requirement]]
id = "PLAN-T-8"
aliases = ["T-8"]
layer = "mechanics"
section = "2. Typing"
title = "Stubs for dynamic surfaces, and *only* for those."
keyword = "MUST NOT"
statement = '''
A module whose public names are bound dynamically MUST ship a `.pyi` that declares them statically — `pybosl2/__init__.pyi`, `shapes2d/__init__.pyi`, `shapes3d/__init__.pyi`, `_shape.pyi`. A module whose functions are ordinary `def`s with full annotations MUST NOT: the stub shadows the truth and can only drift from it. `solid.pyi` was exactly that and had — it declared `cube` with five parameters where the module has fifteen, so `cube(size=20, chamfer=2)` failed the checker while working perfectly, and the docstring example that wrote it was reported as the defect. It is deleted.

A stub and its module MUST NOT drift; parity is enforced by `tests/test_init_stub.py`, and the docstring-example gate (D-P5a) is what catches the drift a parity test does not model.
'''
enforced_by = [
    "tests/test_init_stub.py::test_stub_exists",
]
candidates = [
    "tests/test_turtle_stub.py::test_the_stub_exists",
]
status = "enforced"

[[requirement]]
id = "PLAN-T-9"
aliases = ["T-9"]
layer = "mechanics"
section = "2. Typing"
title = "No dynamic globals."
statement = '''
Never `globals()[name] = …` or `setattr(module, …)` to register an API. The one permitted `__getattr__` is the top-level lazy re-export table, which is backed by its stub (T-8).
'''
enforced_by = [
    "tests/test_code_hygiene.py::test_no_api_is_registered_dynamically",
]
status = "enforced"
note = '''
The lazy-export cache in `__init__.py` is T-9's own carve-out and is named as the one exception.
'''

[[requirement]]
id = "PLAN-T-9a"
aliases = ["T-9a"]
layer = "mechanics"
section = "2. Typing"
title = "Keyword-only is a `*` in the signature."
statement = '''
Everything past the subject argument goes after a bare `*` (SPEC D-1), so no caller can depend on positional order for shaping, placement, resolution or escape-hatch parameters. A public callable carries at most one required parameter; a second needs a `Note:` in the docstring saying why no default can invent it (SPEC D-2), and a third is never acceptable.
'''
enforced_by = [
    "tests/test_signatures.py::test_no_file_grows_its_positional_tier_parameters",
]
status = "enforced"
note = '''
The same scan as SPEC P-5 and D-1.
'''

[[requirement]]
id = "PLAN-T-9b"
aliases = ["T-9b"]
layer = "mechanics"
section = "2. Typing"
title = '`None` means "decide for me", never "off".'
statement = '''
"Off" is `0`, `False`, or an enum member (`ScrewDriveType.NONE`) — SPEC D-4. A parameter whose `None` means "do not apply this" is a defect: it makes "unset" and "disabled" indistinguishable, which is what breaks ambient defaults (§4.3) and `effective_defaults()`.
'''
enforced_by = []
status = "reviewed"
note = '''
Whether a given `None` means 'decide for me' or 'off' is a reading of intent. T31's JUSTIFIED_NONE records the façade's cases with reasons, which is as close as a test gets.
'''

[[requirement]]
id = "PLAN-T-10"
aliases = ["T-10"]
layer = "mechanics"
section = "2. Typing"
title = "Immutable defaults."
statement = '''
Parameter defaults are `None`, numbers, strings, tuples, or enum members — never a list, dict, or set, in `.py` or `.pyi` (SPEC D-3). Guarded by `tests/test_defaults.py::test_no_mutable_defaults_anywhere_in_the_package`.
'''
enforced_by = [
    "tests/test_defaults.py::test_no_mutable_defaults_anywhere_in_the_package",
]
status = "enforced"

[[requirement]]
id = "PLAN-O-0"
aliases = ["O-0"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Every subsystem is on the map."
statement = '''
SPEC §6 catalogues the subsystems this library owns — paths and curves, regions, meshes and surfaces, sweeps, strokes and caps, masks, partitions, distribution, textures, colour, import/interchange, parts. New work belongs to one of them; if it does not, the spec gains a subsection before the code lands.
'''
enforced_by = []
status = "reviewed"
note = '''
'New work belongs to a subsystem, and if it does not the spec gains a subsection first' is a process rule for a reviewer, not a property of the tree.
'''

[[requirement]]
id = "PLAN-O-0a"
aliases = ["O-0a"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Parts build through the façade."
statement = '''
A part imports `pybosl2.solid` / `pybosl2.flat`, never `pybosl2.shapes3d` / `pybosl2.shapes2d` directly, so it honours the active backend (SPEC S-46a). Reaching a backend module directly is what makes a part silently CSG-only.
'''
enforced_by = [
    "tests/test_parts_backend_coverage.py",
]
status = "enforced"
note = '''
A part reaching a backend module directly shows up as a part that cannot build on SDF, which is what this walks.
'''

[[requirement]]
id = "PLAN-O-1"
aliases = ["O-1"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Parts are classes."
keyword = "MAY"
statement = '''
Every entry in `pybosl2/parts/` is a class — `Screw`, `SpurGear`, `KnuckleHinge`, `RegularPolyhedron`, `NemaMotor` — not a family of functions. A stateless family of catalogue lookups MAY instead be a class holding classmethod factories (`BallBearings.ball_bearing("608")`).
'''
enforced_by = [
    "tests/test_exports.py::test_every_part_exposes_shape_as_a_property",
]
candidates = [
    "tests/turtle/test_turtle_methods.py",
]
status = "enforced"
note = '''
Every entry in `pybosl2.parts.__all__` is walked and required to be a class.
'''

[[requirement]]
id = "PLAN-O-1a"
aliases = ["O-1a"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Prefixed function families are factories on a class."
statement = '''
A group of free functions sharing a prefix and a parameter list is an argument bag with extra steps (SPEC P-8): give the group a class and make each member a factory on it — `Metaball.sphere(...)`, `Mask2D.roundover(...)`, `Mask3D.chamfer(...)`. The old names stay as **aliases of the new members** (`mask2d_roundover = Mask2D.roundover`), never as a second copy of the body, so the two spellings cannot diverge.
'''
enforced_by = []
status = "reviewed"
note = '''
Whether a prefixed group of functions is 'a family' is the judgement -- Mask2D, Metaball and the turtles were, and a scan for shared prefixes would not have told them from coincidence.
'''

[[requirement]]
id = "PLAN-O-1c"
aliases = ["O-1c"]
layer = "mechanics"
section = "3. Objects over functions"
title = "A mixin is inherited or it is deleted."
keyword = "MUST"
statement = '''
Where a class publishes operations through a mixin, that mixin MUST be in the class's MRO — never re-implemented in the class alongside it. A duplicate is dead on one side: `partitions.Partitionable` held all nine cut operators while `shapes3d/base.py` carried its own copy of every one, so the mixin was documented, tested and never executed, and the two copies had already drifted. If a mixin looks redundant, delete it; do not leave two implementations of one operation.
'''
enforced_by = [
    "tests/test_partitions.py::test_bosl2_solid_gets_its_cuts_from_the_partitions_mixin",
]
status = "enforced"
note = '''
The mixin is required to be in the MRO, which is what the duplicate copy defeated.
'''

[[requirement]]
id = "PLAN-O-1b"
aliases = ["O-1b"]
layer = "mechanics"
section = "3. Objects over functions"
title = "A command enum plus a parameter dataclass gets methods too."
statement = '''
Where an API is driven by `Thing(CommandType.MOVE, size=40)` values, the object executing them also exposes one method per command, generated from a single table so parallel implementations cannot drift (`TurtleCommands`, mixed into `Turtle2D` and `Turtle3D`). The command objects remain the substrate — the methods build them — so generated programs still work.
'''
enforced_by = [
    "tests/turtle/test_turtle_methods.py",
]
candidates = [
    "tests/test_turtle_stub.py",
]
status = "enforced"
note = '''
The generated per-command methods are checked against the command table they come from.
'''

[[requirement]]
id = "PLAN-O-2"
aliases = ["O-2"]
layer = "mechanics"
section = "3. Objects over functions"
title = "The standard part shape."
statement = '''
A part class has: 1. a `__init__` taking the catalogue name / defining dimension first and everything else defaulted (SPEC P-1), 2. a resolved, frozen **spec object** — `ScrewSpec`, `GearSpec`, `BearingSpec`, `NemaSpec` — built on the first line of `__init__`, 3. read-only `@property` accessors for every derived dimension (`diameter`, `pitch`, `pitch_radius`, `head_height`) so callers can measure without building geometry, 4. a **`@property` named `shape`** returning the `Solid`/`Flat`, built on first access and cached in a private attribute — `part.shape`, never `part.shape()`. **The validation stays in `__init__`.** A part separates three things, not two: validate the arguments, resolve the derived dimensions, then build. Deferring the validation along with the geometry means a rejected call stops raising until someone asks for a shape, which is E-4 and E-5 undone — the caller learns about their mistake at a call they did not make. `Slider` (`pybosl2/parts/sliders.py`) is the worked example; `tests/test_parts_are_lazy.py` counts what is left, 5. a `show()` for the interactive/preview case.

Beware the name: on a backend wrapper, `.shape` is the *raw native handle* (SPEC C-14a). A part's `shape` returns the wrapped `Solid`; a wrapper's returns what it wraps.
'''
enforced_by = [
    "tests/test_exports.py::test_every_part_exposes_shape_as_a_property",
]
candidates = [
    "tests/test_exports.py::test_every_part_exposes_shape_as_a_property",
    "tests/test_parts_are_lazy.py::test_no_part_becomes_eager",
]
status = "enforced"

[[requirement]]
id = "PLAN-O-3"
aliases = ["O-3"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Geometry types are classes too."
statement = '''
`Path2D`, `Path3D`, `Region`, `VNF`, `Bezier`, `NurbsCurve`, `Color`, `Bounds2D/3D` own their operations as methods, so work reads as a fluent chain rather than nested calls.
'''
enforced_by = [
    "tests/test_paths.py",
]
status = "enforced"
note = '''
The geometry types' methods are exercised as methods returning new objects.
'''

[[requirement]]
id = "PLAN-O-4"
aliases = ["O-4"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Free functions are for shape constructors and pure maths."
statement = '''
`cuboid()`, `circle()`, `slerp()`, `frag_count()` stay functions; anything with state, derived values, or a catalogue behind it becomes a class.
'''
enforced_by = []
status = "reviewed"
note = '''
Which callables should stay functions is the converse judgement to O-1a, and no more checkable.
'''

[[requirement]]
id = "PLAN-O-5"
aliases = ["O-5"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Immutability."
statement = '''
Spec objects are frozen dataclasses. Shapes and paths return new objects from every operation (SPEC C-3); no public method mutates `self`. The pattern is copy-then-set — `out = self._wrap(self.shape); out.tag_name = …; return out` — so the `attachments`/`tag_name` setters exist for the copy to use and are never part of the public call surface.
'''
enforced_by = [
    "tests/test_declared_surface.py::test_every_spec_object_is_frozen",
]
status = "enforced"
note = '''
`ScrewSpec` is the one exception, with the reason: an 87-line constructor that parses a trade name, so converting it is a refactor rather than a decorator.
'''

[[requirement]]
id = "PLAN-O-5b"
aliases = ["O-5b"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Derived values are computed, then exposed."
statement = '''
A value that follows from the others is computed once — in the frozen spec object or on the first line of `__init__` — and surfaced as a read-only property, never demanded from the caller (SPEC P-3, D-6). `ScrewSpec` deriving pitch from `"M6"` and `GearSpec` deriving pitch radius from teeth+module are the patterns to copy.
'''
enforced_by = []
status = "reviewed"
note = '''
That a derived value *is* derived rather than demanded is P-3's judgement in Python spelling.
'''

[[requirement]]
id = "PLAN-O-6"
aliases = ["O-6"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Enums for closed vocabularies."
statement = '''
`StrEnum` in `pybosl2/enums.py` and `pybosl2/parts/enums.py`, so the legacy string spelling still compares equal. A new bare-string parameter with a fixed set of accepted values is a defect.
'''
enforced_by = []
status = "reviewed"
note = '''
New bare-string parameters with a fixed set of values are the defect; nothing can tell a closed vocabulary from an open one by looking.
'''

[[requirement]]
id = "PLAN-O-5a"
aliases = ["O-5a"]
layer = "mechanics"
section = "3. Objects over functions"
title = "`show()` returns the shape."
statement = '''
Every implementation of `show()` returns `Self`, never `None` (SPEC S-49), so it can close a chain: `part = cuboid([20, 20, 10]).up(5).show()`. A builder's `show()` delegates to its `shape`'s and returns the same way.
'''
enforced_by = [
    "tests/test_exports.py::test_part_show_returns_the_shape",
]
status = "enforced"

[[requirement]]
id = "PLAN-O-6a"
aliases = ["O-6a"]
layer = "mechanics"
section = "3. Objects over functions"
title = "The backend tag is a class constant."
keyword = "MUST NOT"
statement = '''
A wrapper declares `backend = "csg"` (or `"sdf"`) as a class attribute; it MUST NOT be assigned from `current_backend()` in `__init__`. The tag says *who built this object*, not *what was selected when it happened* (SPEC C-1), and reading the ambient value there silently disables the cross-backend guard.
'''
enforced_by = [
    "tests/test_backend_matrix.py::test_a_csg_shape_built_inside_an_sdf_block_still_says_csg",
]
status = "enforced"
note = '''
Reading the ambient backend into the tag is exactly what this fails on.
'''

[[requirement]]
id = "PLAN-O-6b"
aliases = ["O-6b"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Anchors use the anchor language."
statement = '''
A parameter meaning "which face, edge or corner" is typed `Anchor | Sequence[float]` and resolved through `resolve_anchor()` (SPEC C-10). Do not accept a bare string like `"top"` or an integer index — that is a parallel vocabulary the reader has to learn twice. `constants.TOP` is an alias of `Anchor.TOP`; new code and examples use the enum spelling (SPEC C-11).
'''
enforced_by = [
    "tests/test_signatures.py::test_every_anchor_parameter_speaks_the_anchor_language",
]
status = "enforced"
note = '''
Found and fixed `text3d`'s `anchor: str = "baseline[-1,0,-1]"` -- the same defect as `flat.text()`'s, in the module the first version of the scan could not see.
'''

[[requirement]]
id = "PLAN-O-6c"
aliases = ["O-6c"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Caps are one value."
statement = '''
A stroke or sweep end treatment is typed `CapType | CapSpec` and normalised once through `caps.norm_caps()` (SPEC S-23, S-24), so `endcap1=`/`endcap2=`/`joints=` each take a single argument rather than a spread of style, size and shape parameters.
'''
enforced_by = [
    "tests/test_drawing.py",
]
status = "enforced"
note = '''
Caps are normalised through one type and exercised as one value by the stroke tests.
'''

[[requirement]]
id = "PLAN-O-7"
aliases = ["O-7"]
layer = "mechanics"
section = "3. Objects over functions"
title = "Composition at the FFI boundary."
statement = '''
`Bosl2Solid`/`Bosl2Shape2D` wrap the native handle by composition, never inheritance, and proxy operations through the explicit `_NATIVE_PASSTHROUGH` allowlist (SPEC C-6).
'''
enforced_by = [
    "tests/test_shape_contract.py::test_the_contract_is_the_whole_object",
]
status = "enforced"
note = '''
Composition rather than inheritance shows in the contract walk; the passthrough allowlist is C-6's half and is not checked.
'''

[[requirement]]
id = "PLAN-F-P1"
aliases = ["F-P1"]
layer = "mechanics"
section = "4.1 Façade constructors"
statement = '''
A façade constructor declares the **real default** for each shared argument in its own signature — `size: float | Sequence[float] = (1, 1, 1)`, `anchor: Anchor = Anchor.CENTER` — and always forwards it. It does not default shared arguments to `None` and hope the backend agrees.
'''
enforced_by = [
    "tests/test_defaults.py::test_the_facade_owns_every_shared_default",
]
status = "enforced"
note = '''
Added in T31; 67 defaults were lifted to make it pass.
'''

[[requirement]]
id = "PLAN-F-P2"
aliases = ["F-P2"]
layer = "mechanics"
section = "4.1 Façade constructors"
statement = '''
Forwarding is filtered by **what the target backend declares**, not by what the caller passed: inspect the backend constructor's signature (cached per callable, as `_takes_res` does) and drop names it does not accept. `given_arguments()` remains the filter for backend-exclusive options only.
'''
enforced_by = [
    "tests/test_defaults.py::test_backends_agree_on_the_defaults_they_share",
]
status = "enforced"

[[requirement]]
id = "PLAN-F-P3"
aliases = ["F-P3"]
layer = "mechanics"
section = "4.1 Façade constructors"
statement = '''
A backend-exclusive option keeps its default on the backend, and the façade documents which backend it applies to (`res` for SDF; `spin`/`orient`/`fn`/`fa`/`fs` for CSG).
'''
enforced_by = []
status = "reviewed"
note = '''
Which options are backend-exclusive is PAR-3's list; that the façade *documents* which backend each applies to is a docstring rule nothing reads.
'''

[[requirement]]
id = "PLAN-F-P4"
aliases = ["F-P4"]
layer = "mechanics"
section = "4.1 Façade constructors"
keyword = "MUST"
statement = '''
`effective_defaults()` MUST keep reporting the truth after any change here — it reads live off the constructor, so the test for it is that it needs no maintenance.
'''
enforced_by = [
    "tests/test_defaults.py::test_effective_defaults_reports_the_real_signature",
]
status = "enforced"
note = '''
It reads live off the constructor, which is what makes the test need no maintenance.
'''

[[requirement]]
id = "PLAN-B-P1"
aliases = ["B-P1"]
layer = "mechanics"
section = "4.2 Backend implementations"
title = "Both backends, or an explicit refusal."
statement = '''
A new shared feature is implemented on both backends, or it raises `UnsupportedByBackendError` **and** is listed in `CSG_ONLY_FEATURES` / `SDF_ONLY_FEATURES` with its reason (SPEC PAR-2, PAR-3). Silently omitting it from one backend is the failure mode those lists exist to prevent.
'''
enforced_by = [
    "tests/test_backend_matrix.py",
]
status = "enforced"
note = '''
A shared feature on one backend only fails the matrix; PAR-3's lists are checked separately.
'''

[[requirement]]
id = "PLAN-B-P2"
aliases = ["B-P2"]
layer = "mechanics"
section = "4.2 Backend implementations"
title = "Name differences live in one table."
statement = '''
Where a backend's own module spells a parameter differently, the mapping goes in that backend's translation table (`SdfBackend._OWN_NAMES`), never into the façade signature (SPEC PAR-4).
'''
enforced_by = []
status = "reviewed"
note = '''
`SdfBackend._OWN_NAMES` is the table and it is used, but nothing forbids a name difference being spelled into the façade signature instead.
'''

[[requirement]]
id = "PLAN-B-P3"
aliases = ["B-P3"]
layer = "mechanics"
section = "4.2 Backend implementations"
title = "Guard operands, do not assume."
statement = '''
Boolean and transform implementations call `check_operand_backend()` before touching an operand, so a mismatch raises `CrossBackendError` with its conversion hint rather than an internal `AssertionError` from deeper in (SPEC C-1, E-3).
'''
enforced_by = [
    "tests/test_backend_crossbackend.py",
]
status = "enforced"
note = '''
A boolean skipping the guard surfaces as an internal error instead of CrossBackendError, which is what this asserts.
'''

[[requirement]]
id = "PLAN-B-P4"
aliases = ["B-P4"]
layer = "mechanics"
section = "4.2 Backend implementations"
title = "An exclusive-feature list is checked, not just written."
statement = '''
Every name in `CSG_ONLY_FEATURES` must be implemented on the SDF shape **as a refusal**, and vice versa (SPEC PAR-3): a real method whose whole body raises `UnsupportedByBackendError` with a hint. The test is that calling it raises and that the message names the way forward — not that the attribute is missing, which would break `isinstance` against the shape protocols (T-6b) and make the refusal less explicit than C-13 asks for. What must never happen is the third case: a name that is listed as exclusive and *works anyway*, so the refusal never fires (as `projection` once did).
'''
enforced_by = [
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
]
candidates = [
    "tests/test_backend_parity.py::test_every_csg_only_feature_refuses_on_the_sdf_shape",
    "tests/test_backend_parity.py::test_the_mesh_operation_list_only_holds_names_that_reach_the_fallback",
]
status = "enforced"

[[requirement]]
id = "PLAN-R-P1"
aliases = ["R-P1"]
layer = "mechanics"
section = "4.3 Resolution plumbing (`fn` / `fa` / `fs` / `res`)"
statement = '''
The parameters are spelled `fn: int | None = None`, `fa: float | None = None`, `fs: float | None = None` (CSG) and `res: int | None = None` (SDF), keyword-only, defaulting to `None`. Never `$fn`, never a positional.
'''
enforced_by = [
    "tests/test_signatures.py::test_every_facet_parameter_is_spelled_the_same_way",
]
status = "enforced"
note = '''
Clean when first measured, so it has no budget: a second spelling would be a new defect.
'''

[[requirement]]
id = "PLAN-R-P2"
aliases = ["R-P2"]
layer = "mechanics"
section = "4.3 Resolution plumbing (`fn` / `fa` / `fs` / `res`)"
keyword = "MUST"
statement = '''
A function that builds an arc, circle, rounding, chamfer arc, sphere, or cylinder MUST declare them, and MUST forward them to every sub-construction it calls — including path helpers (`round_corners`, `arc_points`), masks, and part sub-assemblies. Dropping them silently half-way down is the defect this rule exists to prevent. Per SPEC R-1a the trigger is *generating points that approximate a curve*: a function that only places or measures from a radius (`arc_copies`, `polar_to_xy`, `circle_circle_tangents`) is out of scope, because nothing it returns has a facet count.
'''
enforced_by = [
    "tests/test_facets.py::test_no_new_curved_api_without_facet_controls",
]
candidates = [
    "tests/test_facets.py",
]
status = "enforced"
note = '''
Declaring them is checked; forwarding them down is the half that is not.
'''

[[requirement]]
id = "PLAN-R-P3"
aliases = ["R-P3"]
layer = "mechanics"
section = "4.3 Resolution plumbing (`fn` / `fa` / `fs` / `res`)"
keyword = "SHOULD"
statement = '''
Resolve them exactly once, at the point of use, through `pybosl2.defaults.resolve_facets()` / `resolve_res()`, which fill in the ambient block values (`use_defaults(fn=64)`). The central resolvers are `_helpers.frag_count()`, `shapes3d/base._ocylinder`/`_osphere`, `shapes2d/circle.circle`, and each backend's `construct()`; new leaf constructors SHOULD route through those rather than re-implementing the `$fa`/`$fs` arithmetic.
'''
enforced_by = []
status = "reviewed"
note = '''
Resolving once at the point of use is a structural habit; nothing counts the resolution sites. G-5 removed the second implementation of the rule itself.
'''

[[requirement]]
id = "PLAN-R-P4"
aliases = ["R-P4"]
layer = "mechanics"
section = "4.3 Resolution plumbing (`fn` / `fa` / `fs` / `res`)"
statement = '''
Never forward a facet argument to a callee that does not declare it — check the signature (as `SdfBackend.construct` does) rather than passing blindly.
'''
enforced_by = [
    "tests/test_backend_matrix.py",
]
status = "enforced"
note = '''
Forwarding a facet argument a backend does not declare is what the tessellation carve-out exists for.
'''

[[requirement]]
id = "PLAN-R-P5"
aliases = ["R-P5"]
layer = "mechanics"
section = "4.3 Resolution plumbing (`fn` / `fa` / `fs` / `res`)"
keyword = "MUST NOT"
statement = '''
New public curved geometry MUST NOT be added to the known-gap list in `tests/test_facets.py`; the list only shrinks.
'''
enforced_by = [
    "tests/test_facets.py::test_the_backlog_only_shrinks",
]
candidates = [
    "tests/test_docstring_examples.py::test_no_module_grows_its_untyped_examples",
    "tests/test_docstring_examples.py::test_there_are_examples_to_check",
]
status = "enforced"

[[requirement]]
id = "PLAN-R-P6"
aliases = ["R-P6"]
layer = "mechanics"
section = "4.3 Resolution plumbing (`fn` / `fa` / `fs` / `res`)"
keyword = "MUST NOT"
statement = '''
`fn=0` is the opt-out from an ambient `fn` (SPEC R-5): `frag_count()` treats `fn < 3` as unset and falls through to `fa`/`fs`. Constructors that document `fn` SHOULD say so, and code MUST NOT treat `0` as a facet count.
'''
enforced_by = [
    "tests/test_defaults.py::test_fn_zero_opts_out_of_an_ambient_fn",
]
status = "enforced"

[[requirement]]
id = "PLAN-D-P1"
aliases = ["D-P1"]
layer = "mechanics"
section = "5. Documentation"
title = "Google style, always."
statement = '''
One-line summary, blank line, then the description. `Args:`, `Returns:`, `Raises:` sections separated by blank lines. No reStructuredText `:param:` fields.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
Ruff's D rules are enabled and run as a gate, which is the mechanical half of Google style.
'''

[[requirement]]
id = "PLAN-D-P2"
aliases = ["D-P2"]
layer = "mechanics"
section = "5. Documentation"
title = "Every public module, class, method, and function has a docstring."
statement = '''
No exceptions for "obvious" properties.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
Ruff's D rules require a docstring on every public callable.
'''

[[requirement]]
id = "PLAN-D-P3"
aliases = ["D-P3"]
layer = "mechanics"
section = "5. Documentation"
title = "No type redundancy."
statement = '''
Types live in the annotations; the prose describes meaning, units, and the default's effect.
'''
enforced_by = []
status = "reviewed"
note = '''
Whether a description repeats the annotation is a reading of the prose.
'''

[[requirement]]
id = "PLAN-D-P4"
aliases = ["D-P4"]
layer = "mechanics"
section = "5. Documentation"
title = "`Raises:` is complete."
statement = '''
Every exception raised in the body appears there, including the `ValueError`s from argument validation.
'''
enforced_by = [
    "tests/test_documented_arguments.py::test_every_public_callable_documents_its_arguments",
]
status = "enforced"
note = '''
Never checked until T35, and the reason to check it was measured rather than assumed: 313 public callables taking 1307 parameters documented none of them. **Zero now** (T39). The 57 blocking SPEC G-4's ambient-resolution documentation went first, the other 256 followed, and the check is a plain assertion rather than a budget. A second, unratcheted check catches a *partial* Args: section, which is a different defect from a missing one.
'''

[[requirement]]
id = "PLAN-D-P4a"
aliases = ["D-P4a"]
layer = "mechanics"
section = "5. Documentation"
title = "The façade is documented like any other API."
statement = '''
A constructor in `solid.py`/`flat.py` carries its own `Args:` for every parameter it declares and its own example — delegating to a backend module documents the backend, not the entry point users are told to use (SPEC DOC-2). Where the resolved default differs per backend, say what it resolves to and point at `effective_defaults()`.
'''
enforced_by = [
    "tests/test_docs_links.py::test_every_facade_callable_documents_its_arguments_and_shows_an_example",
]
candidates = [
    "tests/test_docs_links.py::test_every_facade_callable_documents_its_arguments_and_shows_an_example",
]
status = "enforced"

[[requirement]]
id = "PLAN-D-P5"
aliases = ["D-P5"]
layer = "mechanics"
section = "5. Documentation"
title = "Examples that build geometry."
keyword = "MUST"
statement = '''
Every function producing 2-D or 3-D output carries a `.. pythonscad-example::` block that renders to STL. A 2-D example MUST extrude (`.linear_extrude(...)`) before `.show()`, since STL has no 2-D form.
'''
enforced_by = [
    "tests/test_docs_links.py::test_every_facade_callable_documents_its_arguments_and_shows_an_example",
]
candidates = [
    "tests/test_docs_links.py::test_every_facade_callable_documents_its_arguments_and_shows_an_example",
]
status = "enforced"

[[requirement]]
id = "PLAN-D-P5a"
aliases = ["D-P5a"]
layer = "mechanics"
section = "5. Documentation"
title = "Every example is type-checked with `mypy --strict`."
statement = '''
SPEC DOC-5, Q-6. An example is user code — it is the code a caller copies — so it is held to exactly what this plan asks of user code, and an example the checker rejects is a **signature defect the docs found first**, to be fixed in the signature rather than worked around in the prose.

The mechanics: `tests/test_docstring_examples.py` walks every public module, extracts each `.. pythonscad-example::` block (and every `>>>` doctest) with the same parser the docs build uses, writes each one to a file in a temp directory, and runs one batched `mypy --strict` over the lot. A failure reports the owning callable, the example, and mypy's message together, so the fix is obvious from the test output alone.

Rules for writing an example so this stays cheap:
* **Imports are real and complete.** The block starts with the `from pybosl2 import …` a caller would need; no implicit names, no `...` elisions, no reliance on a doc-build preamble.
* **No `# type: ignore`.** An example is the wrong place to suppress an error — if the example needs one, the signature is wrong (that is the point of the rule).
* **Annotate only where inference genuinely fails.** Examples should read like a user wrote them, not like a stub file.
* A block that is deliberately not runnable Python — a shell command, a `.scad` fragment shown for comparison — uses a plain `code-block` directive, not `pythonscad-example`, so it is not collected.

Because it type-checks rather than executes, the gate needs no CAD runtime and runs in CI. It is not a substitute for D-P5's rendering — one proves the example *compiles*, the other that it *builds something*.
'''
enforced_by = [
    "tests/test_docstring_examples.py::test_no_example_suppresses_a_type_error",
]
candidates = [
    "tests/test_docstring_examples.py::test_no_example_suppresses_a_type_error",
    "tests/test_docstring_examples.py::test_no_module_grows_its_untyped_examples",
]
status = "enforced"

[[requirement]]
id = "PLAN-D-P5b"
aliases = ["D-P5b"]
layer = "mechanics"
section = "5. Documentation"
title = "An example that states a value is held to it."
statement = '''
A trailing comment on a printed expression is an assertion in prose:

```python
  print(bracket.bounds().size)  # (60.0, 40.0, 18.0)
```

Nothing else checks it. D-P5a proves the line *compiles*; D-P5 renders the *geometry* and never reads the printed text. So a number written from intuition rather than from running the API survives both — which is how the getting-started page came to claim `(60, 40, 18)` for a bracket that measures `(60, 40, 12)`, `attach()` having recorded the boss rather than merged it.

`tests/test_example_claims.py` executes every example carrying such a comment and compares. Examples without one are left alone: the rule is to check claims, not to run everything. Write the comment only for a value you have actually seen — and when it disagrees with the code, run the example before deciding which is wrong, because that is how the wrong one got written.
'''
enforced_by = [
    "tests/test_example_claims.py",
]
status = "enforced"
note = '''
Every example stating a value in a trailing comment is executed and held to it.
'''

[[requirement]]
id = "PLAN-D-P6"
aliases = ["D-P6"]
layer = "mechanics"
section = "5. Documentation"
title = "File header tags."
statement = '''
Immediately after the licence header, every module carries:

```
  # LibFile: pybosl2/<name>.py          # required for public modules
  # FileSummary: <one line, < 120 chars>  # required for public modules
  # DocCategory: <section>              # required
  # FileGroup: BOSL2
```

`DocCategory` is one of `Foundational`, `Paths, regions & surfaces`, `Math & geometry`, `Parts library`, `Extras`, `Solid backends`, or `internal` (excluded from the public docs).
'''
enforced_by = [
    "tests/test_docs_links.py",
]
status = "enforced"
note = '''
The header tags are read and required.
'''

[[requirement]]
id = "PLAN-D-P6a"
aliases = ["D-P6a"]
layer = "mechanics"
section = "5. Documentation"
keyword = "MUST NOT"
statement = '''
A module that holds **any** publicly exported name MUST NOT be `internal` — that tag means "support code", and using it on a module users import silently drops a whole subsystem from the reference (SPEC S-47). The test: if the name appears in `pybosl2/__init__.pyi`, its module needs a public category, a `LibFile` and a `FileSummary`.
'''
enforced_by = [
    "tests/test_docs_links.py",
]
status = "enforced"
note = '''
A module holding a top-level export is required to have a public category.
'''

[[requirement]]
id = "PLAN-D-P7"
aliases = ["D-P7"]
layer = "mechanics"
section = "5. Documentation"
statement = '''
Docs are generated from these headers and docstrings by `docs/_rstgen.py`, the visual catalogue by `docs/_specgen.py`, and the BOSL2 coverage table by `docs/_covgen.py`. Never hand-maintain a list of APIs in `.rst` — a generated page carries a "generated by" line, is committed alongside its generator, and has a test asserting the two still agree, so a forgotten re-run fails the build instead of publishing a stale page.
'''
enforced_by = [
    "tests/test_bosl2_coverage.py::test_the_committed_page_matches_the_generator",
]
status = "enforced"
note = '''
Extended to SPEC/PLAN by tests/test_reqgen.py in T27.
'''

[[requirement]]
id = "PLAN-M-1"
aliases = ["M-1"]
layer = "mechanics"
section = "6. Modules, imports, and laziness"
statement = '''
One module per ported `.scad` file, named after it, so the Python and OpenSCAD sources read side by side. Support modules are `_`-prefixed and marked `DocCategory: internal`.
'''
enforced_by = []
status = "reviewed"
note = '''
One module per ported .scad file is a naming convention; B2-1's coverage table records which file each module ports, but nothing enforces the shape of new ones.
'''

[[requirement]]
id = "PLAN-M-2"
aliases = ["M-2"]
layer = "mechanics"
section = "6. Modules, imports, and laziness"
statement = '''
Every public module declares `__all__`, and **every name in it resolves** (SPEC A-7) — a stale entry breaks `import *` and every tool that walks the list.
'''
enforced_by = [
    "tests/test_exports.py::test_every_all_entry_resolves",
]
candidates = [
    "tests/test_exports.py",
]
status = "enforced"

[[requirement]]
id = "PLAN-M-2a"
aliases = ["M-2a"]
layer = "mechanics"
section = "6. Modules, imports, and laziness"
statement = '''
The top-level `_LAZY_EXPORTS` points at the **façade** (`pybosl2.solid`, `pybosl2.flat`), never at a backend module, so no top-level name can build on a backend the caller did not select (SPEC A-6). A shape only one backend can make is reached through that backend's module, or gets a façade constructor that refuses on the other.
'''
enforced_by = [
    "tests/test_exports.py::test_no_top_level_name_builds_on_the_wrong_backend",
]
status = "enforced"

[[requirement]]
id = "PLAN-M-3"
aliases = ["M-3"]
layer = "mechanics"
section = "6. Modules, imports, and laziness"
keyword = "MUST"
statement = '''
No native runtime at import time. PythonSCAD primitives are reached through `pybosl2._native.native("cube")`, which defers `import pythonscad` to the first call; libfive is a lazy handle in `pybosl2/sdf/_libfive.py`. `import pybosl2` MUST work with neither installed.
'''
enforced_by = [
    "tests/test_export.py::test_writing_a_mesh_touches_no_native_module",
]
status = "enforced"
note = '''
Import-time native access is what this stubs out and would fail on.
'''

[[requirement]]
id = "PLAN-M-4"
aliases = ["M-4"]
layer = "mechanics"
section = "6. Modules, imports, and laziness"
statement = '''
Type-only imports go under `if TYPE_CHECKING:`.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
Ruff's TC rules move type-only imports into the block and run as a gate.
'''

[[requirement]]
id = "PLAN-M-5"
aliases = ["M-5"]
layer = "mechanics"
section = "6. Modules, imports, and laziness"
statement = '''
Import direction follows the spec's layering (SPEC A-1); a cycle is resolved by moving the shared piece down a layer, not by a function-local import — those are reserved for genuine cycles and are commented when used.
'''
enforced_by = [
    "tests/test_layering.py::test_every_deferred_upward_import_is_a_cycle_or_known_debt",
]
status = "enforced"
note = '''
T29: a deferred import must break a real cycle, and the test resolves the cycle rather than accepting the claim.
'''

[[requirement]]
id = "PLAN-E-P1"
aliases = ["E-P1"]
layer = "mechanics"
section = "7. Errors"
statement = '''
Raise `ValueError` for bad arguments, naming the parameter(s) and the accepted spellings: *"tube(): needs two of the three sizes — an inner radius/diameter, an outer radius/diameter, and a wall thickness."*
'''
enforced_by = [
    "tests/test_validation_messages.py",
]
candidates = [
    "tests/test_validation_messages.py",
]
status = "enforced"
note = '''
The message is asserted, not just the type.
'''

[[requirement]]
id = "PLAN-E-P2"
aliases = ["E-P2"]
layer = "mechanics"
section = "7. Errors"
statement = '''
`assert` is for internal invariants only. An `assert` that a user's argument combination is valid is a defect: asserts vanish under `python -O`, and `AssertionError` tells the caller nothing about what to pass. The test for which is which: **if the message names a parameter the caller typed, it is validation and must raise `ValueError`**; if it states something the code guarantees for itself ("cells are non-empty here"), it is an invariant and may stay an assert with no user-facing wording. Two corollaries, each with its own ratchet in `tests/test_defaults.py`:
* A **message-less** `assert` on a parameter is validation too — it is the worst form, since `python -O` erases it and the caller gets wrong geometry instead of an error. It is allowed only where it *narrows* a value the function already validated, which shows as an earlier `raise` whose message names that parameter.
* **`raise AssertionError(...)` is never right.** It survives `-O`, so it is not even an assert's equivalent, and it reports the caller's mistake as an internal bug. Bad input raises `ValueError`; a broken invariant uses `assert`.
'''
enforced_by = [
    "tests/test_defaults.py::test_no_assert_validates_user_input",
]
candidates = [
    "tests/test_defaults.py::test_no_assert_validates_user_input",
    "tests/test_defaults.py::test_no_bare_assert_stands_in_for_validation",
]
status = "enforced"

[[requirement]]
id = "PLAN-E-P3"
aliases = ["E-P3"]
layer = "mechanics"
section = "7. Errors"
keyword = "MUST"
statement = '''
Library errors derive from `pybosl2.exceptions.Bosl2Error`; backend refusals use `UnsupportedByBackendError(feature, backend, hint=…)` and cross-backend mixing uses `CrossBackendError`. Both MUST carry an actionable hint.
'''
enforced_by = [
    "tests/test_error_contract.py::test_the_package_raises_no_bare_value_error",
]
status = "enforced"

[[requirement]]
id = "PLAN-E-P4"
aliases = ["E-P4"]
layer = "mechanics"
section = "7. Errors"
statement = '''
Never swallow an exception to return a degenerate shape. A constructor that cannot produce a usable shape from what it was given raises (SPEC E-5); it does not return a polygon of coincident points, a zero-size solid, or anything whose `bounds()` is infinite or `NaN`. `tests/test_min_args.py` asserts the *result*, not merely the absence of a traceback: a zero-argument call either raises `ValueError` or produces a shape with finite, positive bounds.
'''
enforced_by = [
    "tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain",
]
candidates = [
    "tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain",
]
status = "enforced"

[[requirement]]
id = "PLAN-E-P4a"
aliases = ["E-P4a"]
layer = "mechanics"
section = "7. Errors"
title = "Validation errors are `Bosl2ValueError`."
statement = '''
SPEC E-1 and E-4 both bind, so the type raised for bad input derives from **both** `Bosl2Error` and `ValueError`:

```python
  class Bosl2ValueError(Bosl2Error, ValueError):
      """Bad input: a ValueError for callers who expect one, a Bosl2Error for callers who catch the family."""
```

Every `raise ValueError(...)` in the package becomes `raise Bosl2ValueError(...)`; the message is unchanged, `except ValueError` keeps working unmodified, and `except Bosl2Error` starts working. Tests that assert `pytest.raises(ValueError)` need no change — that is the point of the MRO. New code raises `Bosl2ValueError`; a bare `ValueError` in the package is a defect, guarded by the same AST ratchet that keeps the `assert`s out (`tests/test_errors.py`).
'''
enforced_by = [
    "tests/test_error_contract.py::test_the_package_raises_no_bare_value_error",
]
candidates = [
    "tests/test_error_contract.py::test_the_package_raises_no_bare_value_error",
]
status = "enforced"

[[requirement]]
id = "PLAN-E-P4b"
aliases = ["E-P4b"]
layer = "mechanics"
section = "7. Errors"
title = "An error raised from `__getattr__` is an `AttributeError` too."
statement = '''
SPEC E-6. `UnsupportedByBackendError` inherits `AttributeError` alongside `Bosl2Error`, because Python's attribute protocol is defined in terms of that type: `hasattr()` and `getattr(x, n, default)` catch `AttributeError` and nothing else, so any other type turns a capability probe into a traceback and breaks `copy`, `pickle`, `inspect` and every REPL completion. Refusals raised from a *call* are unaffected by the change — the extra base is invisible unless something is probing.
'''
enforced_by = [
    "tests/test_error_contract.py::test_hasattr_on_an_unknown_name_answers_rather_than_raising",
]
status = "enforced"

[[requirement]]
id = "PLAN-E-P5"
aliases = ["E-P5"]
layer = "mechanics"
section = "7. Errors"
statement = '''
Resolve radius/diameter spellings through `_helpers.pick_radius()`, never with a hand-rolled `radius if radius is not None else diameter / 2` ternary. The helper is what enforces SPEC D-5 — same-dimension conflicts raise, specificity levels resolve most-specific-first — and a local ternary silently opts out of it.
'''
enforced_by = [
    "tests/test_defaults.py::test_a_radius_and_its_own_diameter_together_are_rejected",
]
status = "enforced"
note = '''
A hand-rolled ternary would not raise, which is what this asserts.
'''

[[requirement]]
id = "PLAN-E-P6"
aliases = ["E-P6"]
layer = "mechanics"
section = "7. Errors"
keyword = "MUST NOT"
statement = '''
An attribute fallback (`__getattr__`) MUST NOT convert a shape to another representation to satisfy a missing method. Implement the method or raise `UnsupportedByBackendError`; meshing a distance field to answer `.up(5)` is a silent, lossy backend conversion (SPEC B-5, C-1).
'''
enforced_by = [
    "tests/test_backend_parity.py::test_an_unknown_operation_refuses_instead_of_meshing",
]
candidates = [
    "tests/test_backend_parity.py::test_sdf_shapes_keep_their_backend_through_moves_and_colour",
]
status = "enforced"

[[requirement]]
id = "PLAN-S-1"
aliases = ["S-1"]
layer = "mechanics"
section = "8. Style"
statement = '''
`ruff format` is the formatter; line length 120; double quotes.
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
`ruff format` is a gate in CI and a pre-commit hook; a misformatted file fails before the suite runs.
'''

[[requirement]]
id = "PLAN-S-2"
aliases = ["S-2"]
layer = "mechanics"
section = "8. Style"
statement = '''
Functions stay short and cohesive — under 50 lines. A longer one is split, not commented into sections.
'''
enforced_by = [
    "tests/test_function_length.py::test_no_file_exceeds_its_budget",
]
status = "enforced"
note = '''
243 functions exceeded 50 lines when this was first measured, the longest at 237. Retiring the rule was the alternative and was rejected: the rule is right, the code had simply never been held to it. It is now a per-file budget that only shrinks, per file rather than one total so a failure names the file you are in.
'''

[[requirement]]
id = "PLAN-S-3"
aliases = ["S-3"]
layer = "mechanics"
section = "8. Style"
statement = '''
No `TODO` comments and no stubbed bodies in committed code. Unfinished work lives in SPEC §12 (conformance), not in the source.
'''
enforced_by = [
    "tests/test_code_hygiene.py::test_no_unfinished_work_hides_in_a_comment",
]
status = "enforced"
note = '''
Both halves: TODO-family comments, and stubbed bodies. Clean when first measured.
'''

[[requirement]]
id = "PLAN-S-4"
aliases = ["S-4"]
layer = "mechanics"
section = "8. Style"
statement = '''
Comments explain *why*, and are kept truthful when the code changes; a comment that describes behaviour the code no longer has is a defect.
'''
enforced_by = []
status = "reviewed"
note = '''
Whether a comment explains *why*, and whether it is still true, is the definition of a review question.
'''

[[requirement]]
id = "PLAN-S-4a"
aliases = ["S-4a"]
layer = "mechanics"
section = "8. Style"
title = "Units are implicit and fixed."
statement = '''
Millimetres, degrees, right-handed Z-up (SPEC D-9). No API takes a `units=` parameter; a caller working in inches multiplies by `constants.INCH` at the call site. Do not introduce a second angular unit either — everything is degrees, and anything ported from a radians-based source converts on entry.
'''
enforced_by = []
status = "reviewed"
note = '''
No API takes a units argument today, but nothing forbids one being added. A scan for a `units=` parameter would make it enforced.
'''

[[requirement]]
id = "PLAN-S-5"
aliases = ["S-5"]
layer = "mechanics"
section = "8. Style"
statement = '''
BOSL2 parameter names are kept unless Python forbids them (`except` → `except_edges`) or a better Python spelling exists (SPEC B-8) — but the *call shape* is Python's, not OpenSCAD's.
'''
enforced_by = []
candidates = [
    "tests/test_exports.py::test_the_named_families_are_exported_whole",
]
status = "reviewed"
note = '''
Which BOSL2 name to keep and which to improve on is a judgement per name; B2-3 is the same rule and the same answer.
'''

[[requirement]]
id = "PLAN-X-1"
aliases = ["X-1"]
layer = "mechanics"
section = "9. Tests"
statement = '''
`pytest` is the runner; tests live in `tests/`, one module per source module.
'''
enforced_by = []
status = "reviewed"
note = '''
A statement about the runner and the layout, true by construction.
'''

[[requirement]]
id = "PLAN-X-2"
aliases = ["X-2"]
layer = "mechanics"
section = "9. Tests"
keyword = "MUST"
statement = '''
Pure-geometry (L0) tests MUST pass with no CAD runtime installed. Tests needing the PythonSCAD **app** (`tests/test_stl_render*.py`) skip when no binary is found via `PYTHONSCAD_BIN` or `/Applications` — they skip, never fail.
'''
enforced_by = [
    "tests/test_no_mesher.py",
]
status = "enforced"
note = '''
The pure-geometry surface is exercised with the mesher stubbed out.
'''

[[requirement]]
id = "PLAN-X-3"
aliases = ["X-3"]
layer = "mechanics"
section = "9. Tests"
statement = '''
Every new public callable gets three tests: it builds *and is measurably right* (X-8), it builds **with the minimum arguments** (this is how SPEC P-1 is enforced mechanically), and its docstring example validates (`tests/validate_examples.py`).
'''
enforced_by = []
status = "unenforced"
note = '''
The minimum-argument third is enforced (Q-4). Nothing requires a *new* callable to arrive with all three tests -- that is what a reviewer counts.
'''

[[requirement]]
id = "PLAN-X-4"
aliases = ["X-4"]
layer = "mechanics"
section = "9. Tests"
keyword = "MUST"
statement = '''
Contract tests that guard the rules in this plan, and MUST keep passing: | Test | Guards | |---|---| | `tests/test_defaults.py::test_no_mutable_defaults_anywhere_in_the_package` | T-10 / SPEC D-3 | | `tests/test_defaults.py::test_argument_free_constructors_either_build_or_explain` | SPEC P-1, E-P2 | | `tests/test_defaults.py::test_facade_never_defaults_an_argument_its_backend_requires` | SPEC P-1 | | `tests/test_defaults.py::test_backends_agree_on_the_defaults_they_share` | SPEC P-2 | | `tests/test_init_stub.py` | T-8 | | `tests/test_facets.py` | R-P2 / R-P5 | | `tests/test_defaults.py::test_a_radius_and_its_own_diameter_together_are_rejected` | E-P5 / SPEC D-5 | | `tests/test_docs_links.py` | D-P6 / D-P6a | | `tests/test_exports.py::test_every_polyline_parameter_accepts_a_path` | T-4 / SPEC C-7 | | `tests/test_bosl2_coverage.py` | D-P7 / SPEC B2-1 | | `tests/test_validation_messages.py` | E-P1 / E-P2 / SPEC E-4 (X-7) | | `tests/test_validation_messages.py::test_no_cover_pragmas_are_attached_to_a_statement` | X-7 |

Tests the rules still need (they land with their tasks in [TASKS.md](TASKS.md)):

| Test to add | Guards | Task | |---|---|---| | a CSG shape built inside `use_backend("sdf")` reports `backend == "csg"` | O-6a / SPEC C-1 | T0 | | no `assert` carries a message naming a caller-supplied parameter | E-P2 / SPEC E-4 | T0b | | no message-less `assert` and no `raise AssertionError` validates input | E-P2 / SPEC E-4 | T10 | | `shape` is a property on every class in `pybosl2.parts.__all__` | O-2 / SPEC C-14 | T0c | | every name in every public `__all__` resolves | M-2 / SPEC A-7 | T0d | | every top-level shape name yields the active backend or refuses | M-2a / SPEC A-6 | T2b | | every part built under `use_backend("sdf")` is SDF-backed or refuses | O-0a / SPEC S-46a | T0f | | every name in `CSG_ONLY_FEATURES` is genuinely absent from the SDF shape | B-P4 / SPEC PAR-3 | T4 | | `tests/test_backend_matrix.py` | SPEC B-7, PAR-2, B-P1 (a new shared feature lands on both backends or is an explicit, tracked refusal) |
'''
enforced_by = [
    "tests/test_ci_gates.py",
]
status = "enforced"
note = '''
The named contract tests run in CI; test_requirements.py now also fails if a rule names one that no longer exists.
'''

[[requirement]]
id = "PLAN-X-7"
aliases = ["X-7"]
layer = "mechanics"
section = "9. Tests"
statement = '''
Every rejection path is exercised, asserting the **message** as well as the type (`tests/test_validation_messages.py`) — E-4 is about the message naming the fix, and an unexercised `raise` is an unverified one. A guard that genuinely cannot fire is marked `# pragma: no cover` **on the `if`/`else` header**, with the reason in a comment below it: coverage matches the pragma against a line, so a marker on its own comment line excludes nothing. `test_no_cover_pragmas_are_attached_to_a_statement` enforces the placement.
'''
enforced_by = [
    "tests/test_validation_messages.py::test_no_cover_pragmas_are_attached_to_a_statement",
]
status = "enforced"

[[requirement]]
id = "PLAN-X-8"
aliases = ["X-8"]
layer = "mechanics"
section = "9. Tests"
title = "A test asserts what the result *is*, never merely that it exists."
keyword = "MUST"
statement = '''
`assert isinstance(result, Bosl2Solid)` passes for every wrong answer that is still a solid: the wrong size, the wrong place, an empty union, a cut that removed everything. It proves the call returned, which the absence of an exception already proved. Every test MUST make at least one assertion about the *content* of what it built:
* **Solids and 2-D shapes** — `bounds()` (centre and size), or a dimension derived from it. A half-cut halves an extent; an offset grows one; a spread separates two centres. These are cheap, exact, and they fail when the geometry is wrong.
* **Paths and regions** — point counts, endpoints, x/y span, closure, winding, perimeter or area. For anything symmetric, assert the symmetry.
* **Meshes** — `VNF` vertex/face counts, signed volume, watertightness.
* **Anything with a backend** — the `backend` tag as well as the type (SPEC C-1).

Measure `bounds()`, never `size`. `size` is the **nominal anchor box** (SPEC S-2a), which is allowed to differ from the geometry — asserting `shape.size == [...]` states what the constructor was handed, not what it built, and passes however wrong the geometry is. Where a test needs both, assert them as two separate claims and say which is which.

A type assertion is a legitimate *addition* to those, and it is the whole point only in a contract test whose subject genuinely is the type — "the façade returns the active backend's class", "a part refuses on the SDF backend". Those state the type claim in the test name.

The cost of getting this wrong is not hypothetical: `pybosl2/partitions.py` was 60% covered with a suite full of `isinstance` checks that all passed while the code they claimed to test was never executed at all (its methods were shadowed by a duplicate), and two of its features were outright broken (see [TASKS.md](TASKS.md) T12).
'''
enforced_by = [
    "tests/test_assertion_quality.py::test_no_test_asserts_only_that_a_result_exists",
]
candidates = [
    "tests/test_assertion_quality.py::test_no_test_asserts_only_that_a_result_exists",
    "tests/test_color.py::test_rainbow_colors_each_object",
    "tests/test_dimension_validation.py",
    "tests/test_masking_primitives.py::test_mask2d_step_encloses_the_notch_it_cuts",
    "tests/test_native_ops.py::test_wrap_returns_solid_with_and_without_fn",
    "tests/test_profiles.py::test_corner_profile_rounds_all_corners",
    "tests/test_regions.py",
    "tests/test_shapes2d_object.py::test_every_constructor_returns_the_2d_wrapper",
    "tests/test_shapes2d_object.py::test_region_geometry_is_the_2d_wrapper",
]
status = "enforced"

[[requirement]]
id = "PLAN-X-5"
aliases = ["X-5"]
layer = "mechanics"
section = "9. Tests"
statement = '''
Changing geometry, backends, or paths means running the full suite — including `pytest tests/test_stl_render.py` when a PythonSCAD binary is available — before the work is called done.
'''
enforced_by = []
status = "reviewed"
note = '''
A process rule about what to run before calling a change done; CI runs the suite regardless, but the render tests skip without a binary.
'''
