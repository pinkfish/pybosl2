# The visual spec-sheet pages are generated by _specgen.py from _extra/specs/_stl/metrics.json at
# docs-build time (see the setup() hook in conf.py). The generator + the committed _stl/ meshes are
# the source of truth; these HTML/CSS files are build artifacts and are not checked in.
/_extra/specs/*.html
/_extra/specs/spec.css
